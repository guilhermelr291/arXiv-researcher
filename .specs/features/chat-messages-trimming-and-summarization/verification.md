# Chat messages trimming and summarization verification

**Verdict**: FAIL
**Profile**: light
**Diff range**: bac717f (uncommitted working tree)
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

## Checks

| Check | Claim | Proof run | Evidence | Result |
| --- | --- | --- | --- | --- |
| C1 | Lifespan source is CREATE-only: `CREATE TABLE IF NOT EXISTS`, primary key `thread_id`, no `DROP` | `-k test_schema_is_create_only_and_lifespan_ensures_it` exit 0 | `tests/test_compaction_store.py:113` - `self.assertIn("CREATE TABLE IF NOT EXISTS", sql)`; `:114` - `self.assertIn("thread_id TEXT PRIMARY KEY", sql)`; `:117` - `self.assertNotIn("DROP", text)` | PASS |
| C2 | Two writes for `t-1` leave 1 row whose summary is `NEW-SUMMARY` | `-k test_second_write_keeps_one_row` exit 0 | `tests/test_compaction_store.py:125` - `self.assertEqual(len(store.rows), 1)`; `:126` - `self.assertEqual(store.rows["t-1"].summary, "NEW-SUMMARY")` | PASS |
| C3 | Store accepts the five statuses, DDL names those five, `paused` is rejected | `-k test_status_set_rejects_paused` exit 0 | `tests/test_compaction_store.py:136` - `self.assertEqual(stored.status, status)` over the literal tuple at `:130`; `:140` - `with self.assertRaises(ValueError)`; `:139` - `self.assertIn(f"'{status}'", sql)` | FAIL — store acceptance is 5/5 (`running`, `ready`, `applied`, `failed`, `discarded`) and `paused` raises. The DDL clause iterates imported `ALLOWED_STATUS`, so the five spellings are not in that assertion expression |
| C4 | No transition to `running` at 48000 tokens, failed age 29s, already `running` (watermark `wm-0`), or already `ready` | `-k test_job_start_refusals` exit 0 | `tests/test_compaction_store.py:204` - `self.assertEqual(row.status, case["expect"])`; `:206` - `self.assertEqual(row.watermark, "wm-0")`; token inputs at `:151` - `"token_count": 8000` and `:152` - `"token_count": 40000` | PASS — 4/4 refusals are in the test function (`applied`, `failed`, `running` / `wm-0`, `discarded`) |
| C5 | Becomes `running` with watermark `wm-1` at 48001 tokens and an 8000-token prefix for absent, `applied`, `discarded`, and `failed` at 30s, without changing messages or summary | `-k test_job_start_sets_running_without_touching_messages` exit 0 | `tests/test_compaction_store.py:241` - `self.assertEqual(row.status, "running")`; `:242` - `self.assertEqual(row.watermark, "wm-1")`; `:243` - `self.assertEqual(state["messages"], before)` | FAIL — 4/4 seeds are in the test (absent, `applied`, `discarded`, `failed` at `clock.t - 30`). Claimed totals 48001 and prefix 8000 are built in `_over_threshold_messages`, not in an assertion |
| C6 | Success on `t-1` sets `ready`, `NEW-SUMMARY`, watermark `wm-2`, error `""` | `-k test_success_overwrites_summary_and_clears_error` exit 0 | `tests/test_compaction_store.py:273` - `self.assertEqual(row.status, "ready")`; `:274` - `self.assertEqual(row.summary, "NEW-SUMMARY")`; `:275` - `self.assertEqual(row.watermark, "wm-2")`; `:276` - `self.assertEqual(row.error, "")` | PASS |
| C7 | Ready row stores token counts 50000, 20000, 400, 12000, and 300 | `-k test_ready_row_stores_five_token_counts` exit 0 | `tests/test_compaction_store.py:302` - `self.assertEqual(row.token_count_before, 50000)`; `:303` - `self.assertEqual(row.estimated_token_count_after, 20000)`; `:304` - `self.assertEqual(row.summary_token_count, 400)`; `:305` - `self.assertEqual(row.summarizer_input_tokens, 12000)`; `:306` - `self.assertEqual(row.summarizer_output_tokens, 300)` | PASS — 5/5 counts |
| C8 | Summarizer call passes `reasoning_effort` `medium` and `max_completion_tokens` 2500 | `-k test_summarizer_reasoning_effort_and_max_tokens` exit 0 | `tests/test_compaction_store.py:313` - `self.assertEqual(ctor.call_args.kwargs["reasoning_effort"], "medium")`; `:314` - `self.assertEqual(ctor.call_args.kwargs["max_completion_tokens"], 2500)` | PASS |
| C9 | Structured output uses the six field names, and the stored summary contains the six headings in that order | `-k test_structured_output_fields_and_headings` exit 0 | `tests/test_compaction_store.py:33` - `_FIELDS = ("current_user_goal", "decisions_made", "constraints_and_preferences", "important_entities_and_values", "what_has_been_done", "open_items")`; `:336` - `self.assertEqual(list(schema.model_fields), list(_FIELDS))`; `:25` - `_HEADINGS` tuple of the six `##` headings; `:350` - `self.assertEqual(positions, sorted(positions))` | PASS — 6/6 fields and 6/6 headings, literals in the test module |
| C10 | Summarizer input includes `PRIOR-GOAL`, `BEFORE-MARK`, and `AT-MARK`, and omits `AFTER-MARK` | `-k test_summarizer_input_is_prefix_through_watermark` exit 0 | `tests/test_compaction_store.py:369` - `self.assertIn("PRIOR-GOAL", prompt)`; `:370` - `self.assertIn("BEFORE-MARK", prompt)`; `:371` - `self.assertIn("AT-MARK", prompt)`; `:372` - `self.assertNotIn("AFTER-MARK", prompt)` | PASS |
| C11 | Summarizer `boom` sets `failed`, the error contains `boom`, and the summary stays `OLD-SUMMARY` | `-k test_summarizer_exception_sets_failed_and_keeps_summary` exit 0 | `tests/test_compaction_store.py:393` - `self.assertEqual(row.status, "failed")`; `:394` - `self.assertIn("boom", row.error)`; `:395` - `self.assertEqual(row.summary, "OLD-SUMMARY")` | PASS |
| C12 | A 90-second timeout sets `failed` and keeps `OLD-SUMMARY` | `-k test_summarizer_timeout_at_90_seconds_sets_failed` exit 0 | `tests/test_compaction_store.py:404` - `self.assertEqual(timeout, 90)`; `:420` - `self.assertEqual(row.status, "failed")`; `:421` - `self.assertEqual(row.summary, "OLD-SUMMARY")` | PASS |
| C13 | Job start and the later `ready` write leave transcript insert, update, and delete counts at 0 | `-k test_compaction_does_not_write_transcript_items` exit 0 | `tests/test_compaction_store.py:437` - `self.assertEqual((spy.inserts, spy.updates, spy.deletes), (0, 0, 0))`; `:444` - `self.assertEqual((spy.inserts, spy.updates, spy.deletes), (0, 0, 0))` | PASS |
| C14 | Registry name `summarizer`, model `gpt-5.6-luna`, tools `()`, absent from `PLAN_AGENTS` and `planner_prompt_abilities()` | `-k test_summarizer_model_and_exclusion_from_plan_agents` exit 0 | `tests/test_compaction_store.py:448` - `self.assertEqual(spec.name, "summarizer")`; `:449` - `self.assertEqual(spec.model, "gpt-5.6-luna")`; `:450` - `self.assertEqual(spec.tools, ())`; `:451` - `self.assertNotIn("summarizer", PLAN_AGENTS)`; `:452` - `self.assertNotIn("summarizer", planner_prompt_abilities())` | PASS |
| C15 | Ready row over 48001 tokens removes `m1` and `m2` and sets `conversation_summary` to `STORED-SUMMARY` | `-k test_ready_over_48000_removes_through_watermark` exit 0 | `tests/test_compaction_apply.py:94` - `self.assertEqual(_ids(result.update["messages"]), ["m1", "m2"])`; `:95` - `self.assertEqual(result.update["conversation_summary"], "STORED-SUMMARY")` | FAIL — removal and summary are asserted. Claimed count 48001 is not in the test function; counts live in `_cut_messages` |
| C16 | After the cut, status is `applied` and the state watermark is `m2` | `-k test_applied_status_and_state_watermark_after_cut` exit 0 | `tests/test_compaction_apply.py:113` - `self.assertEqual(row.status, "applied")`; `:114` - `self.assertEqual(result.update["applied_watermark"], "m2")` | PASS |
| C17 | Message `m3` remains after the cut | `-k test_message_after_watermark_remains` exit 0 | `tests/test_compaction_apply.py:126` - `self.assertIn("m3", _ids(merged))` | PASS |
| C18 | At 0 and at 48000 tokens, ids stay put and status stays `ready` | `-k test_ready_at_or_under_48000_does_not_remove` exit 0 | `tests/test_compaction_apply.py:129` - `for total in (0, 48000)`; `:147` - `self.assertNotIn("messages", result.update)`; `:150` - `self.assertEqual(row.status, "ready")`; `:151` - `self.assertEqual(_ids(messages), before)` | PASS — 2/2 totals |
| C19 | Missing watermark `missing` becomes `discarded`, ids stay `m1`, summary stays `KEEP` | `-k test_missing_watermark_is_discarded` exit 0 | `tests/test_compaction_apply.py:164` - `self.assertEqual(row.status, "discarded")`; `:165` - `self.assertEqual(_ids(messages), ["m1"])`; `:167` - `self.assertEqual(state["conversation_summary"], "KEEP")` | PASS |
| C20 | Base `old-mark` versus state `other-mark` becomes `discarded`, ids unchanged, summary stays `KEEP` | `-k test_base_watermark_mismatch_is_discarded` exit 0 | `tests/test_compaction_apply.py:180` - `self.assertEqual(row.status, "discarded")`; `:181` - `self.assertEqual(_ids(messages), ["m1", "m2", "m3"])`; `:183` - `self.assertEqual(state["conversation_summary"], "KEEP")` | PASS |
| C21 | Ready at 48001 tokens with applied watermark already `m2` becomes `applied` and keeps `m1` | `-k test_already_applied_watermark_does_not_remove` exit 0 | `tests/test_compaction_apply.py:200` - `self.assertEqual(row.status, "applied")`; `:202` - `self.assertIn("m1", _ids(messages))` | FAIL — status `applied` and `m1` are asserted. Claimed count 48001 is in `_cut_messages`, and the row watermark `m2` is the `_ready()` default, outside the assertion |
| C22 | A non-resume update stream visits `compact` before `gate` | `-k test_fresh_run_visits_compact_before_gate` exit 0 | `tests/test_compaction_graph.py:70` - `self.assertLess(nodes.index("compact"), nodes.index("gate"))` | PASS |
| C23 | Resume `POST /agent` calls `astream` with input `None`, and that stream has no `compact` update | `-k test_resume_calls_astream_with_none` and `-k test_resume_astream_none_skips_compact` exit 0 | `tests/test_agent_route.py:192` - `self.assertIsNone(fake.recorded["input"])`; `tests/test_compaction_graph.py:90` - `self.assertNotIn("compact", second)` | PASS — both proofs printed `ok`. The route file is unchanged from bac717f; its assertion is the check value `None` on this tree. The skip-`compact` proof is in the new graph test |
| C24 | Planner prompt places `<conversation_summary>` after the instruction prefix and before `Query:`, with `GOAL-42` inside the tag, and includes `user-1` | `-k test_planner_summary_before_query_and_all_messages` exit 0 | `tests/test_history_prompts.py:179` - `self.assertLess(tag, query)`; `:180` - `self.assertIn("Produce an ordered executable plan", blob[:tag])`; `:181` - `self.assertIn("GOAL-42", blob[tag:query])`; `:182` - `self.assertIn("user-1", blob)` | PASS |
| C25 | Planner prompt has no `<conversation_summary>` when the summary is `""` and when the key is absent | `-k test_planner_omits_summary_tag_when_empty` exit 0 | `tests/test_history_prompts.py:185` - `for summary in ("", None)`; `:207` - `self.assertNotIn("<conversation_summary>", blob)` | PASS — 2/2 (empty string, and `None` omits the key) |
| C26 | Gate prompt includes the last 6 of 8 messages, omits the first 2, and has neither the tag nor `GOAL-42` | `-k test_gate_last_six_messages_omit_summary` exit 0 | `tests/test_history_prompts.py:237` - `self.assertIn(f"msg-{i}", blob)` for `i` in `range(2, 8)`; `:238` - `self.assertNotIn("msg-0", blob)`; `:239` - `self.assertNotIn("msg-1", blob)`; `:240` - `self.assertNotIn("<conversation_summary>", blob)`; `:241` - `self.assertNotIn("GOAL-42", blob)` | PASS — 6/6 kept, 2/2 omitted |
| C27 | Writer prompt includes the last 2 exchanges, omits the first 2, and has neither the tag nor `GOAL-42` | `-k test_writer_two_exchanges_omit_summary` exit 0 | `tests/test_history_prompts.py:276` - `self.assertIn("user-3", blob)`; `:277` - `self.assertIn("user-4", blob)`; `:278` - `self.assertNotIn("user-1", blob)`; `:279` - `self.assertNotIn("user-2", blob)`; `:280` - `self.assertNotIn("<conversation_summary>", blob)`; `:281` - `self.assertNotIn("GOAL-42", blob)` | PASS |
| C28 | `replan_remaining` contains neither `<conversation_summary>` nor `GOAL-42` | `-k test_replan_remaining_omits_summary` exit 0 | `tests/test_history_prompts.py:304` - `self.assertNotIn("<conversation_summary>", blob)`; `:305` - `self.assertNotIn("GOAL-42", blob)` | PASS |
| C29 | Kept suffix is 16000 tokens, starts on a user message, and the watermark is the id immediately before that suffix | `-k test_watermark_keeps_16000_tokens_from_a_user_message` exit 0 | `tests/test_compaction_apply.py:212` - `self.assertEqual(choice.suffix_tokens, 16000)`; `:213` - `self.assertEqual(messages[choice.suffix_index]["role"], "user")`; `:215` - `self.assertEqual(messages[choice.suffix_index - 1]["id"], choice.watermark_id)` | PASS |
| C30 | When the 16000-token tail starts on an assistant message, the suffix starts on the preceding user message and totals more than 16000 | `-k test_watermark_extends_suffix_to_a_user_message` exit 0 | `tests/test_compaction_apply.py:225` - `self.assertEqual(messages[choice.suffix_index]["role"], "user")`; `:226` - `self.assertGreater(choice.suffix_tokens, 16000)` | PASS |
| C31 | Prefix of 7999 tokens, or no user message, does not become `running` and does not change the message count | `-k test_short_prefix_or_no_user_boundary_does_not_cut` exit 0 | `tests/test_compaction_apply.py:231` - `"token_count": 7999`; `:255` - `self.assertTrue(row is None or row.status != "running")`; `:257` - `self.assertEqual(len(merged), before)` | PASS — 2/2 (`prefix-7999`, `no-user`) |
| C32 | Count uses `o200k_base` over the summary plus messages, omits system text and papers, and `Policy.chunk_encoding` stays `cl100k_base` | `-k test_token_count_uses_o200k_base_and_omits_system_and_papers` exit 0 | `tests/test_compaction_apply.py:274` - `tiktoken.get_encoding("o200k_base")`; `:275` - `expected = len(enc.encode(summary)) + len(enc.encode(user))`; `:276` - `self.assertEqual(thread_token_count(state), expected)`; `:279` - `self.assertEqual(Policy.chunk_encoding, "cl100k_base")` | PASS — summary, user message, system text, and papers are all in the test function; equality to `expected` excludes system and papers |
| C33 | A stored token count of 100 is reused when `o200k_base` on the text is a different count | `-k test_stored_message_token_count_is_reused` exit 0 | `tests/test_compaction_apply.py:286` - `self.assertNotEqual(len(enc.encode(text)), 100)`; `:291` - `self.assertEqual(thread_token_count(state), 100)` | PASS |
| C34 | The cut emits `RemoveMessage` for `m1` and `m2`, and `add_messages` keeps `m3` | `-k test_remove_message_ids_applied_by_add_messages` exit 0 | `tests/test_compaction_apply.py:305` - `self.assertIsInstance(message, RemoveMessage)`; `:306` - `self.assertEqual(message.id, mid)` over `("m1", "m2")`; `:308` - `self.assertEqual(_ids(merged), ["m3"])` | PASS — 2/2 ids |
| C35 | A graph with no compaction store enters `gate`, leaves message ids, writes no row; the recall script calls `ResearchGraph` with `checkpointer=None` and passes no store | `-k test_missing_store_enters_gate` and `-k test_recall_script_has_no_compaction_store` exit 0 | `tests/test_compaction_graph.py:101` - `self.assertIn("gate", nodes)`; `:102` - `self.assertEqual(state["messages"][0]["id"], "m1")`; `:103` - `self.assertEqual(store.rows, {})`; `:108` - `"ResearchGraph(deps, checkpointer=None, halt_before_writer=True)"` | FAIL — `store` is created at `:93` and `ResearchGraph(_deps())` at `:94` never receives it, so `store.rows == {}` does not observe a compaction write. The script proof does settle `checkpointer=None` and `self.assertNotIn("compaction", text)` at `:111` |
| C36 | `initial_graph_state("q")` has `conversation_summary` `""` and applied watermark `""` | `-k test_initial_state_summary_and_watermark_empty` exit 0 | `tests/test_research_graph.py:86` - `self.assertEqual(state["conversation_summary"], "")`; `:87` - `self.assertEqual(state["applied_watermark"], "")` | PASS |
| C37 | A checkpoint that omits the summary and the watermark, with no row, leaves ids unchanged and does not raise | `-k test_omitted_checkpoint_fields_mean_no_summary` exit 0 | `tests/test_compaction_apply.py:315` - `self.assertNotIn("messages", result.update)`; `:316` - `self.assertEqual(_ids(messages), ["m1", "m2"])`; `:317` - `self.assertEqual(store.rows, {})` | PASS |
| C38 | Running for 91s becomes `failed`; 90s stays `running`; 29s after failure stays `failed`; 30s after failure becomes `running` | `-k test_stale_running_fails_and_cooldown_is_30_seconds` exit 0 | `tests/test_compaction_apply.py:344` - `self.assertEqual(stayed.status, "running")` after `clock.t - 90`; `:359` - `self.assertEqual(failed.status, "failed")` after `clock.t - 91`; `:369` - `self.assertEqual(row.status, "failed")` at `failed_at + 29`; `:380` - `self.assertEqual(row.status, "running")` at `failed_at + 30` | PASS — 4/4 ages |
| C39 | Compact returns while status is `running` and `outcome` is `pending`; after the summarizer resolves, status is `ready` and `outcome` is still `pending` | `-k test_summarizer_ready_after_turn_returns` exit 0 | `tests/test_compaction_apply.py:406` - `self.assertEqual(row.status, "running")`; `:407` - `self.assertEqual(state["outcome"], "pending")`; `:414` - `self.assertEqual(row.status, "ready")`; `:415` - `self.assertEqual(state["outcome"], "pending")` | PASS |
| C40 | After the turn returns, a summarizer raise leaves `failed`, summary `OLD-SUMMARY`, and `outcome` `pending` | `-k test_summarizer_failure_after_turn_keeps_outcome_pending` exit 0 | `tests/test_compaction_apply.py:443` - `self.assertEqual(row.status, "failed")`; `:444` - `self.assertEqual(row.summary, "OLD-SUMMARY")`; `:445` - `self.assertEqual(state["outcome"], "pending")` | PASS |

## Swept existing

Authorization row: `existing - CORS WEB_ORIGIN on POST /agent; no auth in v1`.

The constraint is in the code. `create_app` installs CORS for the app that mounts the router: `src/plan_based_researcher/main.py:77` - `install_cors(app, web_origin())`. The origin is `src/plan_based_researcher/config.py:15` - `return os.environ.get("WEB_ORIGIN", DEFAULT_WEB_ORIGIN)`. `POST /agent` depends on `get_graph`, `get_settings`, and `get_transcript` only: `src/plan_based_researcher/api/routes.py:35`.

## Gate

`uv run python -m unittest -v tests.test_compaction_store tests.test_compaction_apply tests.test_compaction_graph tests.test_history_prompts tests.test_research_graph tests.test_agent_route` with one `-k` per named test (42 names, including both C23 proofs and both C35 proofs).

Ran 42 tests in 0.888s — OK (42 passed, 0 failed).

A single `-k` pattern joined with `|` ran 0 tests (unittest exit 5). Unittest `-k` is a fnmatch, so `|` is literal. The counts above are the one-process run in which every named test printed `ok`.

## Ranked gaps

1. C35 — `tests/test_compaction_graph.py:93` — the empty-row assertion watches a store the graph is not given.
2. C15 — `tests/test_compaction_apply.py:94` — 48001 is not in the assertion; counts are in `_cut_messages`.
3. C5 — `tests/test_compaction_store.py:241` — 48001 and prefix 8000 are in `_over_threshold_messages`, not in the assertion.
4. C21 — `tests/test_compaction_apply.py:200` — 48001 and the row watermark `m2` are outside the assertion.
5. C3 — `tests/test_compaction_store.py:139` — DDL membership iterates imported `ALLOWED_STATUS`.
