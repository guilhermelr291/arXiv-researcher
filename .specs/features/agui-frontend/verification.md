# AG-UI transport + Next.js research desk verification

**Verdict**: FAIL
**Profile**: light
**Diff range**: 34b248e..WORKING-TREE
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

Profile is **light**: every named proof was run at the working tree (`34b248e` plus uncommitted feature files); each named test exists in the tree and appeared individually as passed; each check C1–C73 has a located `file:line`; level/sampling gaps are recorded; Swept `existing` was re-read against the code. Binding sources were **not** opened (step 1 is `ui` only). Coverage was **not** recomputed from authority. Faults were **not** injected; no git worktree; the tree was not mutated. UI was **not** walked with a human (C42–C69 settled by named vitest proofs). No `standard`/`ui` steps ran.

## Checks

Batched proofs (working tree):

`uv run python -m unittest tests.test_finalize tests.test_writer_stream tests.test_thread_messages tests.test_history_prompts tests.test_writer_thread_evidence tests.test_pending_next tests.test_agent_route tests.test_agui_adapter tests.test_threads_route tests.test_agui_removal tests.test_agui_docs -v` — exit 0, **62** passed, **0** failed. Every named Python proof appeared as `ok`: `test_done_appends_aimessage_with_writer_id`, `test_non_done_appends_aimessage_with_reason`, `test_aimessage_metadata_key_set`, `test_plan_item_projection_fields`, `test_answer_start_before_first_delta`, `test_citations_per_turn_fields`, `test_second_run_keeps_first_turn`, `test_gate_and_planner_window_is_six`, `test_writer_window_is_two_plus_evidence`, `test_planner_lists_admitted_papers_and_omit_search`, `test_empty_chunks_renumber_prior_citations`, `test_empty_chunks_no_citations_insufficient`, `test_new_input_discards_pending_and_starts`, `test_valid_run_starts_with_run_started`, `test_invalid_body_is_422`, `test_empty_messages_without_resume_is_400`, `test_resume_idle_is_409`, `test_resume_calls_astream_with_none`, `test_iterator_raise_is_run_error`, `test_timeout_is_run_finished_insufficient`, `test_frames_use_event_encoder`, `test_agent_stream_headers`, `test_cors_allows_web_origin`, `test_options_agent_is_200`, `test_research_route_absent_health_present`, `test_consume_path_is_astream_custom_updates`, `test_step_started_for_every_node`, `test_search_execute_step_started_metadata`, `test_step_finished_query_used_from_step_end`, `test_parallel_search_step_indexes_differ`, `test_gate_event_is_activity_snapshot`, `test_plan_event_is_pending_snapshot`, `test_plan_delta_patch_same_message_id`, `test_eval_updates_plan_item_status`, `test_answer_start_is_text_message_start`, `test_answer_delta_is_text_message_content`, `test_citations_end_then_sources`, `test_run_finished_reason_null_when_done`, `test_get_thread_200_shape`, `test_empty_thread_is_404`, `test_human_maps_to_user_message`, `test_done_replay_order`, `test_non_done_replay_is_outcome_not_assistant`, `test_status_interrupted_vs_idle`, `test_replay_uses_project_turn`, `test_old_transport_paths_absent`, `test_pyproject_drops_chainlit_adds_ag_ui`, `test_no_test_module_imports_removed_paths`, `test_discover_count_not_below_190`, `test_agents_md_agent_threads_web_invariants`. Extra tests in the same modules also ran `ok` (not named as proofs).

`npm --prefix web exec -- vitest run` — exit 0, **29** passed (default reporter did not print titles). The same tree under `--reporter=verbose` listed every C42–C69 title as passed (C67 contributes two titles). Titles: `home renders input and description without fetch`, `first send posts agent and replaceState`, `same-tab first send skips get threads`, `direct thread load waits for replay`, `thread 404 navigates home`, `recents upsert title eighty chars`, `stop aborts fetch and input stays enabled`, `dropped stream refetches thread`, `interrupted auto-resumes once then shows resume`, `interrupted strip uses warn and does not block input`, `refused and insufficient render reason only`, `run error renders warn marginalia`, `normal run one user message resume empty`, `live step rail current accent previous ok`, `finished step rail collapses to count and seconds`, `search rail shows query used`, `gate is one marginalia line`, `plan list survives delta then collapses`, `matching n is source button links stay links`, `unknown n is plain text`, `citation popover delay 150 stay 300`, `citation click opens panel over sidebar`, `no sources list after assistant`, `user rule two px accent assistant unboxed`, `type tokens serif 17 ui inter mono 12`, `tokens css has light and dark sets`, `components contain no hex colour literals`, `reduced motion disables transitions and cursor`, `no gradient avatar bubble shimmer spinner`.

All named Python/vitest files are in `git diff 34b248e` / untracked working-tree (new or modified).

| Check | Claim | Proof run | Evidence | Result |
| --- | --- | --- | --- | --- |
| C1 | finalize `done` appends AIMessage content=`writer_markdown`, id=`writer_message_id` | `test_done_appends_aimessage_with_writer_id` ok | `tests/test_finalize.py:132` - `self.assertEqual(getattr(message, "content", None), "Grounded answer [1]")`; `:133` `self.assertEqual(getattr(message, "id", None), "msg-writer-1")` | PASS |
| C2 | finalize `refused`/`insufficient`/`error` appends AIMessage reason + `response_metadata.outcome`, table-driven 3 | `test_non_done_appends_aimessage_with_reason` ok | `tests/test_finalize.py:158` - `self.assertEqual(getattr(message, "content", None), reason)`; `:160` `self.assertEqual(metadata.get("outcome"), outcome)` (`subTest` over the 3 outcomes) | PASS |
| C3 | AIMessage metadata keys `outcome,gate,plan,steps,citations`; plan items `index,agent,task,status,feedback`; steps `count,elapsed_ms` | `test_aimessage_metadata_key_set` ok; `test_plan_item_projection_fields` ok | `tests/test_finalize.py:167` - `self.assertEqual(list(metadata), ["outcome", "gate", "plan", "steps", "citations"])`; `:179` `self.assertEqual(set(item), {"index", "agent", "task", "status", "feedback"})`; `:184` `self.assertEqual(set(steps), {"count", "elapsed_ms"})` | PASS |
| C4 | `answer_start` with uuid `message_id` before first `answer_delta`; returned as `writer_message_id` | `test_answer_start_before_first_delta` ok | `tests/test_writer_stream.py:177` - `self.assertGreater(events.index("answer_delta"), events.index("answer_start"))`; `:181` `self.assertEqual(len(message_id), 36)`; `:182` `self.assertEqual(result["writer_message_id"], message_id)` | PASS |
| C5 | second run on same `thread_id` keeps first Human+AI then second pair | `test_second_run_keeps_first_turn` ok | `tests/test_thread_messages.py:57` - `self.assertEqual(contents[:4], ["first", "answer to first", "second", "answer to second"])` | PASS |
| C6 | gate/planner human payload last `Policy.history_window_exchanges=6`; omit exchanges 7–8 of 8, both agents | `test_gate_and_planner_window_is_six` ok | `tests/test_history_prompts.py:43` - `self.assertEqual(Policy.history_window_exchanges, 6)`; `:84` `self.assertIn(f"user-{i}", blob)` for i in 3..8; `:85` `self.assertNotIn("user-1", blob)`; `:86` `self.assertNotIn("user-2", blob)` | PASS |
| C7 | Writer prompt last `Policy.writer_history_exchanges=2` plus evidence; omit older of 4 | `test_writer_window_is_two_plus_evidence` ok | `tests/test_history_prompts.py:89` - `self.assertEqual(Policy.writer_history_exchanges, 2)`; `:121` `self.assertIn("user-3", blob)`; `:123` `self.assertNotIn("user-1", blob)`; `:125` `self.assertIn("[1]", blob)`; `:126` `self.assertIn("low-rank adaptation excerpt", blob)` | PASS |
| C8 | planner lists admitted arxiv id+title and omit-search wording | `test_planner_lists_admitted_papers_and_omit_search` ok | `tests/test_history_prompts.py:152` - `self.assertIn("2401.00001", blob)`; `:153` `self.assertIn("LoRA: Low-Rank Adaptation", blob)`; `:156` `self.assertIn("omit search", blob.lower())`; `:157` `self.assertIn("already admitted", blob.lower())` | PASS |
| C9 | empty chunks + prior citations → evidence from `[1]` in message order, dedup `chunk_id` | `test_empty_chunks_renumber_prior_citations` ok | `tests/test_writer_thread_evidence.py:76` - `self.assertIn("[1]", blob)`; `:80` `self.assertNotIn("later copy of dup", blob)`; `:87` `self.assertEqual(len(ids), len(set(ids)))` | PASS |
| C10 | empty chunks + no prior citations → `outcome="insufficient"`, Writer model not called | `test_empty_chunks_no_citations_insufficient` ok | `tests/test_writer_thread_evidence.py:109` - `self.assertEqual(result["outcome"], "insufficient")`; `:110` `llm.astream.assert_not_called()` | PASS |
| C11 | citations numbered from 1; fields `n,chunk_id,arxiv_id,title,year,url,excerpt` table-driven 7 | `test_citations_per_turn_fields` ok | `tests/test_writer_stream.py:204` - `self.assertEqual(citations[0]["n"], 1)`; `:207` `self.assertIn(field, citations[0])` (`subTest` over the 7 fields) | PASS |
| C12 | new input on non-empty checkpoint `next` starts from START; pending node does not run | `test_new_input_discards_pending_and_starts` ok | `tests/test_pending_next.py:46` - `self.assertNotIn("pending:one", ran)`; `:47` `self.assertIn("first:two", ran)` | PASS |
| C13 | `POST /agent` valid body → `200` `text/event-stream`; first frame `RUN_STARTED` with request `threadId`/`runId` | `test_valid_run_starts_with_run_started` ok | `tests/test_agent_route.py:102` - `self.assertEqual(response.status_code, 200)`; `:103` `self.assertTrue(response.headers["content-type"].startswith("text/event-stream"))`; `:105` `self.assertEqual(events[0]["type"], "RUN_STARTED")`; `:106` `self.assertEqual(events[0]["threadId"], "tid-1")`; `:107` `self.assertEqual(events[0]["runId"], "run-1")` | PASS |
| C14 | invalid `RunAgentInput` body → `422` with validation detail | `test_invalid_body_is_422` ok | `tests/test_agent_route.py:112` - `self.assertEqual(response.status_code, 422)`; `:113` `self.assertTrue(response.json().get("detail"))` | PASS |
| C15 | empty `messages` without resume → `400` `{"detail":"one user message or resume is required"}` | `test_empty_messages_without_resume_is_400` ok | `tests/test_agent_route.py:122` - `self.assertEqual(response.status_code, 400)`; `:123` `self.assertEqual(response.json(), {"detail": "one user message or resume is required"})` | PASS |
| C16 | resume true + empty `next` → `409` `{"detail":"nothing to resume"}` | `test_resume_idle_is_409` ok | `tests/test_agent_route.py:135` - `self.assertEqual(response.status_code, 409)`; `:136` `self.assertEqual(response.json(), {"detail": "nothing to resume"})` | PASS |
| C17 | resume true + non-empty `next` → `astream` input `None` on that `thread_id` | `test_resume_calls_astream_with_none` ok | `tests/test_agent_route.py:151` - `self.assertIsNone(fake.recorded["input"])`; `:152` `self.assertEqual(fake.recorded["config"]["configurable"]["thread_id"], "tid-1")` | PASS |
| C18 | adapter `graph.astream(..., stream_mode=["custom","updates"])`; source has no `astream_events` | `test_consume_path_is_astream_custom_updates` ok | `tests/test_agui_adapter.py:69` - `self.assertIn('stream_mode=STREAM_MODES', source)`; `:70` `self.assertEqual(STREAM_MODES, ["custom", "updates"])`; `:71` `self.assertNotIn("astream_events", source)` | PASS |
| C19 | `STEP_STARTED` for all 8 node names; `search`/`execute` metadata `{step_index, agent, task}` | `test_step_started_for_every_node` ok; `test_search_execute_step_started_metadata` ok | `tests/test_agui_adapter.py:101` - `self.assertEqual([e["stepName"] for e in started], list(names))` with `names` the 8 nodes; `:127` `self.assertEqual(started["metadata"]["step_index"], 0)`; `:128` `self.assertEqual(started["metadata"]["agent"], name)`; `:129` `self.assertEqual(started["metadata"]["task"], f"{name} task")` | PASS |
| C20 | `STEP_FINISHED` same `step_name`; `search`/`execute` `metadata.query_used` from `step_end` | `test_step_finished_query_used_from_step_end` ok | `tests/test_agui_adapter.py:151` - `self.assertEqual(finished["stepName"], "search")`; `:152` `self.assertEqual(finished["metadata"]["query_used"], "ti:LoRA")` | PASS |
| C21 | two `search` `STEP_STARTED` in one Send wave have differing `metadata.step_index` | `test_parallel_search_step_indexes_differ` ok | `tests/test_agui_adapter.py:190` - `self.assertEqual(len(started), 2)`; `:192` `self.assertEqual(indexes, {0, 1})` | PASS |
| C22 | gate custom → `ACTIVITY_SNAPSHOT` `GATE` content `{in_domain, language, reason}` | `test_gate_event_is_activity_snapshot` ok | `tests/test_agui_adapter.py:212` - `self.assertEqual(snap["type"], "ACTIVITY_SNAPSHOT")`; `:213` `self.assertEqual(snap["content"], {"inDomain": True, "language": "en", "reason": "ok"} if "inDomain" in snap["content"] else {"in_domain": True, "language": "en", "reason": "ok"})`; `:219` `self.assertEqual(snap["content"]["reason"], "ok")` | PASS |
| C23 | plan event → `ACTIVITY_SNAPSHOT` `PLAN` items `{index, agent, task, status:"pending", feedback:null}` | `test_plan_event_is_pending_snapshot` ok | `tests/test_agui_adapter.py:241` - `self.assertEqual(item["index"], 0)`; `:242` `self.assertEqual(item["agent"], "search")`; `:243` `self.assertEqual(item["task"], "Find papers")`; `:244` `self.assertEqual(item["status"], "pending")`; `:245` `self.assertIsNone(item.get("feedback"))` | PASS |
| C24 | plan `updates` → `ACTIVITY_DELTA` `PLAN` JSON Patch on same `message_id` as snapshot | `test_plan_delta_patch_same_message_id` ok | `tests/test_agui_adapter.py:277` - `self.assertEqual(delta["activityType"], "PLAN")`; `:278` `self.assertEqual(delta["messageId"], snap["messageId"])`; `:279` `self.assertTrue(delta["patch"])` | PASS |
| C25 | eval event sets plan item `feedback` (English) and status `passed`/`retry`/`replan`, table-driven 3 | `test_eval_updates_plan_item_status` ok | `tests/test_agui_adapter.py:327` - `self.assertEqual(items[0]["status"], expected)` (`pass`→`passed`, `retry`, `replan`); `:328` `self.assertEqual(items[0]["feedback"], "english feedback")` | PASS |
| C26 | `answer_start` → `TEXT_MESSAGE_START` `message_id` + `role="assistant"` | `test_answer_start_is_text_message_start` ok | `tests/test_agui_adapter.py:342` - `self.assertEqual(start["messageId"], mid)`; `:343` `self.assertEqual(start["role"], "assistant")` | PASS |
| C27 | `answer_delta` → `TEXT_MESSAGE_CONTENT` `delta`=text on that `message_id` | `test_answer_delta_is_text_message_content` ok | `tests/test_agui_adapter.py:355` - `self.assertEqual(content["delta"], "Hello")`; `:356` `self.assertEqual(content["messageId"], mid)` | PASS |
| C28 | citations → `TEXT_MESSAGE_END` then `ACTIVITY_SNAPSHOT` `SOURCES` items 7 fields in `n` order | `test_citations_end_then_sources` ok | `tests/test_agui_adapter.py:377` - `end_i = types.index("TEXT_MESSAGE_END")`; `:379` `self.assertGreater(types.index("ACTIVITY_SNAPSHOT", end_i), end_i)`; `:384` `self.assertIn(field, item)` / `:385` `self.assertEqual(item[field], citation[field])` for the 7 fields | PASS |
| C29 | finalize done → `RUN_FINISHED` `result.{outcome,reason}` with `reason` null when `done` | `test_run_finished_reason_null_when_done` ok | `tests/test_agui_adapter.py:393` - `self.assertEqual(finished["result"]["outcome"], "done")`; `:394` `self.assertIsNone(finished["result"].get("reason"))` | PASS |
| C30 | graph iterator raise → `RUN_ERROR` `message=str(exc)` then close | `test_iterator_raise_is_run_error` ok | `tests/test_agent_route.py:163` - `error = next(e for e in events if e["type"] == "RUN_ERROR")`; `:164` `self.assertEqual(error["message"], "boom")`; `:165` `self.assertFalse(response.text.endswith("incomplete"))` | PASS |
| C31 | timeout → `RUN_FINISHED` `result.{outcome:"insufficient", reason:"timeout"}` then close | `test_timeout_is_run_finished_insufficient` ok | `tests/test_agent_route.py:172` - `self.assertEqual(finished["result"]["outcome"], "insufficient")`; `:173` `self.assertEqual(finished["result"]["reason"], "timeout")` | PASS |
| C32 | frames encoded with `ag_ui.encoder.EventEncoder`; `Cache-Control: no-cache`; `X-Accel-Buffering: no` | `test_frames_use_event_encoder` ok; `test_agent_stream_headers` ok | `tests/test_agent_route.py:178` - `self.assertIn("data: {", response.text)` / `:179` `self.assertIn('"type":"RUN_STARTED"', ...)`; `:186` `self.assertEqual(response.headers[key], value)` looping `stream_headers().items()` — neither assertion names `EventEncoder`, `no-cache`, or `X-Accel-Buffering` | FAIL |
| C33 | `Origin`=`WEB_ORIGIN` (default `http://localhost:3000`) → `Access-Control-Allow-Origin`; `OPTIONS /agent` `200` | `test_cors_allows_web_origin` ok; `test_options_agent_is_200` ok | `tests/test_agent_route.py:196` - `self.assertEqual(response.headers.get("access-control-allow-origin"), DEFAULT_WEB_ORIGIN)`; `:210` `self.assertEqual(response.status_code, 200)` | PASS |
| C34 | `POST /research` not registered; `GET /health` remains | `test_research_route_absent_health_present` ok | `tests/test_agent_route.py:215` - `self.assertEqual(missing.status_code, 404)`; `:217` `self.assertEqual(health.status_code, 200)`; `:223` `self.assertIn("/health", text)`; `:224` `self.assertNotIn("/research", text)` | PASS |
| C35 | `GET /threads/{thread_id}` with messages → `200` `{threadId, messages, status}` | `test_get_thread_200_shape` ok | `tests/test_threads_route.py:105` - `self.assertEqual(response.status_code, 200)`; `:107` `self.assertEqual(set(body), {"threadId", "messages", "status"})` | PASS |
| C36 | empty checkpoint → `404` `{"detail":"thread not found"}` | `test_empty_thread_is_404` ok | `tests/test_threads_route.py:116` - `self.assertEqual(response.status_code, 404)`; `:117` `self.assertEqual(response.json(), {"detail": "thread not found"})` | PASS |
| C37 | user dict / `HumanMessage` → `UserMessage` same `id` and `content` | `test_human_maps_to_user_message` ok | `tests/test_threads_route.py:123` - `self.assertEqual(mapped[0].role, "user")`; `:124` `self.assertEqual(mapped[0].id, "h-keep")`; `:125` `self.assertEqual(mapped[0].content, "hello there")`; `:129` `self.assertEqual(mapped_dict[0].id, "h-dict")`; `:130` `self.assertEqual(mapped_dict[0].content, "from dict")` | PASS |
| C38 | done replay order GATE, PLAN, STEPS `{count,elapsed_ms}`, AssistantMessage id+content, SOURCES | `test_done_replay_order` ok | `tests/test_threads_route.py:157` - `self.assertEqual(order, ["GATE", "PLAN", "STEPS", "assistant", "SOURCES"])`; `:159` `self.assertEqual(assistant.id, "ai-done")`; `:160` `self.assertEqual(assistant.content, "answer [1]")`; `:162` `self.assertEqual(steps.content["count"], 2)`; `:163` `self.assertEqual(steps.content["elapsed_ms"], 40)` | PASS |
| C39 | non-done replay GATE (when present), OUTCOME `{outcome,reason}`, no AssistantMessage, table-driven 3 | `test_non_done_replay_is_outcome_not_assistant` ok | `tests/test_threads_route.py:175` - `self.assertNotIn("assistant", roles)`; `:177` `self.assertEqual(types[0], "GATE")`; `:178` `self.assertEqual(types[-1], "OUTCOME")`; `:180` `self.assertEqual(outcome_msg.content["outcome"], outcome)` | PASS |
| C40 | replay `status` `"interrupted"` iff snapshot `next` non-empty else `"idle"` | `test_status_interrupted_vs_idle` ok | `tests/test_threads_route.py:216` - `self.assertEqual(interrupted.json()["status"], "interrupted")`; `:228` `self.assertEqual(idle.json()["status"], "idle")` | PASS |
| C41 | replay PLAN/SOURCES contents from same `project_turn` the live adapter uses | `test_replay_uses_project_turn` ok | `tests/test_threads_route.py:233` - `self.assertIs(replay_mod.project_turn, project_turn)`; `:253` `self.assertEqual(plan.content["items"], projected["plan"])`; `:254` `self.assertEqual(sources.content["items"], projected["citations"])` | PASS |
| C42 | `/` renders input + one description line; no `fetch` | `home renders input and description without fetch` ok | `web/lib/desk.test.tsx:48` - `expect(screen.getByLabelText("Message")).toBeTruthy()`; `:49` `expect(screen.getByText(/Ask an AI\/ML question/i)).toBeTruthy()`; `:50` `expect(fetch).not.toHaveBeenCalled()` | PASS |
| C43 | first send: uuid v4 `threadId`, `POST` `/agent`, `replaceState` `/c/{threadId}` | `first send posts agent and replaceState` ok | `web/lib/desk.test.tsx:62` - `expect(url).toContain("/agent")`; `:63` `expect(replace).toHaveBeenCalled()`; `:64` `expect(String(replace.mock.calls[0][2])).toMatch(/\/c\/[0-9a-f-]{36}/i)` | PASS |
| C44 | same-tab first send does not `GET /threads/{threadId}` | `same-tab first send skips get threads` ok | `web/lib/desk.test.tsx:74` - `expect(urls.every((url) => !url.includes("/threads/"))).toBe(true)` | PASS |
| C45 | open `/c/{threadId}` calls `GET /threads/{threadId}` and renders messages before input enabled | `direct thread load waits for replay` ok | `web/lib/desk.test.tsx:86` - `expect(screen.getByLabelText("Message")).toBeDisabled()`; `:97` `expect(screen.getByText("hello")).toBeTruthy()`; `:98` `expect(screen.getByLabelText("Message")).not.toBeDisabled()` — no assertion that `fetch` is `GET /threads/{threadId}` | FAIL |
| C46 | thread `404` navigates to `/` | `thread 404 navigates home` ok | `web/lib/desk.test.tsx:106` - `await waitFor(() => expect(replace).toHaveBeenCalledWith("/"))` | PASS |
| C47 | first user message upserts `{threadId, title, updatedAt}` with title first 80 chars | `recents upsert title eighty chars` ok | `web/lib/desk.test.tsx:112` - `expect(rows[0].title).toHaveLength(80)`; `:113` `expect(rows[0].threadId).toBe("tid")`; `:114` `expect(rows[0].updatedAt).toBeTruthy()` | PASS |
| C48 | while streaming, input enabled; Stop aborts via `AbortController.abort` | `stop aborts fetch and input stays enabled` ok | `web/lib/desk.test.tsx:131` - `expect(screen.getByLabelText("Message")).not.toBeDisabled()`; `:133` `await waitFor(() => expect(aborted).toBe(true))` | PASS |
| C49 | stream without `RUN_FINISHED`/`RUN_ERROR` → `GET /threads/{threadId}` and re-render | `dropped stream refetches thread` ok | `web/lib/desk.test.tsx:153` - `expect(String(vi.mocked(fetch).mock.calls[1][0])).toContain("/threads/")`; `:154` `await waitFor(() => expect(screen.getByText("replayed")).toBeTruthy())` | PASS |
| C50 | interrupted → one auto-resume after 1000 ms; second drop shows Resume | `interrupted auto-resumes once then shows resume` ok | `web/lib/desk.test.tsx:176` - `await vi.advanceTimersByTimeAsync(1000)`; `:184` `expect(vi.mocked(fetch).mock.calls.length).toBe(3)`; `:185` `expect(screen.getByText("Resume")).toBeTruthy()` | PASS |
| C51 | interrupted strip uses `--warn` above input; typing stays enabled | `interrupted strip uses warn and does not block input` ok | `web/lib/desk.test.tsx:196` - `expect(\`${getComputedStyle(strip).color} ${strip.className}\`).toMatch(/warn\\|interrupt/)`; `:197` `expect(screen.getByLabelText("Message")).not.toBeDisabled()` — fixture HTML, not `Chat` with `status="interrupted"`; `--warn` is not the asserted value (`interrupt-strip` matches `interrupt`) | FAIL |
| C52 | `RUN_FINISHED` `refused`/`insufficient` reason as marginalia, no assistant text, both | `refused and insufficient render reason only` ok | `web/lib/desk.test.tsx:206` - `expect(next.blocks.some((b) => b.kind === "outcome" && b.reason === \`${outcome} because\`)).toBe(true)`; `:207` `expect(next.blocks.some((b) => b.kind === "assistant")).toBe(false)` | PASS |
| C53 | `RUN_ERROR` `message` as marginalia using `--warn` | `run error renders warn marginalia` ok | `web/lib/desk.test.tsx:219` - `expect(line.className).toMatch(/warn/)` with `getByText("boom")` | PASS |
| C54 | normal run one `UserMessage`; resume empty `messages` + `forwardedProps.resume=true` | `normal run one user message resume empty` ok | `web/lib/desk.test.tsx:225` - `expect(normal.messages).toHaveLength(1)`; `:226` `expect(normal.messages[0].role).toBe("user")`; `:228` `expect(resume.messages).toEqual([])`; `:229` `expect(resume.forwardedProps.resume).toBe(true)` | PASS |
| C55 | live StepRail: current node mono `--accent`; previous dots `--ok` | `live step rail current accent previous ok` ok | `web/lib/desk.test.tsx:241` - `expect(container.querySelector(".current")?.className).toMatch(/current/)`; `:243` `expect(container.querySelector(".current")?.textContent).toContain("planner")` | PASS |
| C56 | `RUN_FINISHED` rail collapses to `"{count} steps · {seconds}s"`; click expands | `finished step rail collapses to count and seconds` ok | `web/lib/desk.test.tsx:248` - `expect(screen.getByText("14 steps · 8.2s")).toBeTruthy()`; `:250` `expect(screen.getByText(/gate/)).toBeTruthy()` after click | PASS |
| C57 | finished search rail shows `metadata.query_used` | `search rail shows query used` ok | `web/lib/desk.test.tsx:262` - `expect(screen.getByText(/ti:LoRA/)).toBeTruthy()` | PASS |
| C58 | GATE is one marginalia line with verdict/reason, not a card | `gate is one marginalia line` ok | `web/lib/desk.test.tsx:273` - `expect(container.querySelectorAll("p.gate")).toHaveLength(1)`; `:274` `expect(container.querySelector(".card")).toBeNull()` | PASS |
| C59 | streaming PLAN numbered list survives delta without remount; finished collapses to one line | `plan list survives delta then collapses` ok | `web/lib/desk.test.tsx:291` - `expect(document.querySelector(".plan-list")).toBe(list)`; `:298` `expect(document.querySelector(".plan-line")).toBeTruthy()` | PASS |
| C60 | matching `[n]` is `<button aria-label="Source n">`; markdown links stay `<a>` | `matching n is source button links stay links` ok | `web/lib/desk.test.tsx:304` - `expect(container.querySelector('button[aria-label="Source 1"]')).toBeTruthy()`; `:305` `expect(container.querySelector("a")?.getAttribute("href")).toBe("https://arxiv.org/abs/1")` | PASS |
| C61 | `[n]` with no SOURCES item is plain text, not a button | `unknown n is plain text` ok | `web/lib/desk.test.tsx:311` - `expect(container.querySelector("button")).toBeNull()`; `:312` `expect(container.textContent).toContain("[99]")` | PASS |
| C62 | citation hover 150 ms / focus opens `--surface` popover; stays 300 ms after leave | `citation popover delay 150 stay 300` ok | `web/lib/desk.test.tsx:321` - `await vi.advanceTimersByTimeAsync(150)` then `:323` `expect(screen.getByText(/arXiv:2401.00001/)).toBeTruthy()`; `:326` `advanceTimersByTimeAsync(299)` still present; `:330` `advanceTimersByTimeAsync(1)` then `:332` `expect(screen.queryByText(/arXiv:2401.00001/)).toBeNull()` | PASS |
| C63 | click citation opens panel from the right over sidebar with excerpt+url; reading column width unchanged | `citation click opens panel over sidebar` ok | `web/lib/desk.test.tsx:344` - `expect(opened[0]).toEqual(source)`; `:347` `expect(getComputedStyle(panel).position \|\| panel.className).toBeTruthy()` — `SourcePanel` is mounted unconditionally; tautology does not target right/over-sidebar/width | FAIL |
| C64 | no sources list after assistant text | `no sources list after assistant` ok | `web/lib/desk.test.tsx:358` - `expect(container.textContent?.includes("Sources")).toBe(false)`; `:359` `expect(container.querySelector("ul.sources")).toBeNull()` | PASS |
| C65 | user message full-width 2 px `--accent` left rule and 16 px inset; assistant unboxed | `user rule two px accent assistant unboxed` ok | `web/lib/desk.test.tsx:374` - `expect(user.className).toContain("user-msg")`; `:375` `expect(container.querySelector(".assistant-text")).toBeTruthy()` — does not assert `2px`, `--accent`, `16px`, or absence of border/background | FAIL |
| C66 | reading serif 17 px / 1.65 max 68ch; UI Inter 13–15 px; mono 12 px | `type tokens serif 17 ui inter mono 12` ok | `web/lib/desk.test.tsx:380` - `expect(css).toMatch(/--reading-size:\s*17px/)`; `:381` `/--reading-leading:\s*1.65/`; `:382` `/--column:\s*68ch/`; `:383` `/Inter/`; `:384` `/--mono-size:\s*12px/` | PASS |
| C67 | colours from `lib/tokens.css` light+dark; components have no hex literals | `tokens css has light and dark sets` ok; `components contain no hex colour literals` ok | `web/lib/desk.test.tsx:390` - `expect(css).toContain("--bg: #fbf9f5")`; `:391` `expect(css).toContain("prefers-color-scheme: dark")`; `:392` `expect(css).toContain("--bg: #14130f")`; `:402` `expect(hex.test(text), name).toBe(false)` | PASS |
| C68 | `prefers-reduced-motion: reduce` disables transitions and streaming cursor | `reduced motion disables transitions and cursor` ok | `web/lib/desk.test.tsx:408` - `expect(css).toContain("prefers-reduced-motion: reduce")`; `:409` `expect(css).toMatch(/transition:\s*none/)`; `:410` `expect(css).toMatch(/\.cursor[\s\S]*display:\s*none/)` | PASS |
| C69 | no CSS gradients, robot avatars, chat bubbles, skeleton shimmer, generic spinner | `no gradient avatar bubble shimmer spinner` ok | `web/lib/desk.test.tsx:421` - `expect(text).not.toMatch(/linear-gradient\|radial-gradient/)`; `:422` `/avatar\|robot/`; `:423` `/chat-bubble\|bubble/`; `:424` `/shimmer\|skeleton/`; `:425` `/spinner/` | PASS |
| C70 | repo lacks `ui/`, `api/sse.py`, `api/stream_dispatcher.py`, `api/executor.py`, table-driven 4 | `test_old_transport_paths_absent` ok | `tests/test_agui_removal.py:25` - `self.assertFalse(path.exists(), path)` over the 4 `_REMOVED` paths | PASS |
| C71 | `pyproject.toml` has no `chainlit`; lists `ag-ui-protocol` | `test_pyproject_drops_chainlit_adds_ag_ui` ok | `tests/test_agui_removal.py:29` - `self.assertNotIn("chainlit", text)`; `:30` `self.assertIn("ag-ui-protocol", text)` | PASS |
| C72 | no test module imports removed paths; discover count ≥ 190 | `test_no_test_module_imports_removed_paths` ok; `test_discover_count_not_below_190` ok | `tests/test_agui_removal.py:43` - `self.assertFalse(any(... banned ...), f"{path.name} imports {node.module}")`; `:58` `self.assertGreaterEqual(count, 190)` | PASS |
| C73 | `AGENTS.md` names `/agent`, `/threads`, `web/`, revised invariants 1, 6, 8, 9 | `test_agents_md_agent_threads_web_invariants` ok | `tests/test_agui_docs.py:14` - `self.assertIn("/agent", text)`; `:15` `self.assertIn("/threads", text)`; `:16` `self.assertIn("web/", text)`; `:25` `self.assertIn(needle, text)` for `web/ is HTTP-only`, `packed in this thread`, `halt_before_writer`, `astream` | PASS |

## Level and sampling

Gaps that do **not** by themselves Unproven a row that still has a settling `file:line` (FAIL rows above are separate):

- **C20** claims every node `STEP_FINISHED` and `search` *and* `execute` `query_used`. Proof is one `search` node. `execute` `query_used` is unexercised.
- **C12** is a tiny `interrupt_before` graph, not `POST /agent` / the product compile. LangGraph default is what ran.
- **C18** is `inspect.getsource` of the adapter module, not a live `astream(..., stream_mode=...)` kwargs capture (C17 records `input`/`config` only).
- **C22** accepts either `in_domain` or `inDomain` at the assertion; the check names `in_domain`.
- **C24** asserts a patch exists and some path contains `status` or `feedback`; it does not assert the patch is *limited* to changed paths.
- **C28** `n` order is one item. **C33** uses `DEFAULT_WEB_ORIGIN` on a local FastAPI+`install_cors` assembly, not `WEB_ORIGIN` env through `create_app`. **C34** HTTP `GET /health` is registered on the test app; product `/health` is a source-string lock on `create_app`.
- **C32** (FAIL): `EventEncoder` is not named at the assertion; header literals live in `stream_headers()` (expected not readable at the assertion — finding about the test).
- **C38/C39/C41** map via `snapshot_to_agui_messages` (C35/C36/C40 hit HTTP). **C41** proves replay imports `project_turn`; it does not inspect the live adapter module for the same symbol.
- **C5** asserts contents order, not `HumanMessage`-shaped dict types. **C4** uuid is length 36, not version-4 nibble. **C1** expected content/id are fixture literals, not the names `writer_markdown` / `writer_message_id` at the assertion.
- **C9** `self.assertEqual(ns, [1, 2] if "[1]" in result["writer_markdown"] else ns)` is tautological when `[1]` is absent; dedup/first-copy assertions still settle the check.
- **C45** (FAIL): hydrate disables input then renders `hello`; the check-defined `GET /threads/{threadId}` is not asserted.
- **C47/C54** hit `upsertRecent` / `normalRunInput`/`resumeRunInput` helpers, not the Chat `fetch` body / first-send path.
- **C51** (FAIL) and **C63** (FAIL): fixture/tautology below the Chat/layout boundary the checks name.
- **C55** current node name is asserted; `--accent`, `--ok`, mono, and previous-as-dot (`•`) are not. **C62** 150/300 ms are asserted; `--surface` and 2–3 line excerpt are not. **C65** (FAIL) class names only. **C66/C68** lock CSS files, not computed styles on the reading column / a `matchMedia` runtime.
- **C50** advances 1000 ms and counts three `fetch` calls; it does not assert the third body is empty+`resume=true` (that shape is C54's helper).
- **C30/C31** “then closes” is implied by a finished TestClient body (`endswith("incomplete")` on C30 only).
- **C72** discover count is asserted `>= 190` inside the named test; the batched runner did not print the integer.

HTTP-boundary claims C13–C17, C30–C34, C35–C36 have TestClient proofs at the route. C38–C39/C41 sit on the replay helper.

## Swept existing

| Row | Cited constraint | In code at working tree? |
| --- | --- | --- |
| validation C14 | invalid `RunAgentInput` → 422 detail | Yes. `src/plan_based_researcher/api/routes.py:34` `body: RunAgentInput` — FastAPI/Pydantic 422 on a non-input body. |
| validation C15 | empty messages without resume → 400 exact detail | Yes. `routes.py:39-42` `HTTPException(status_code=400, detail="one user message or resume is required")`. |
| failure C10 | empty evidence + no prior citations → insufficient, no Writer model | Yes. `src/plan_based_researcher/agents/writer.py:358-364` `prior_citation_chunks`; empty → `return {"outcome": "insufficient", ...}` before `self._llm.astream`. |
| failure C30 | iterator raise → `RUN_ERROR` then close | Yes. `src/plan_based_researcher/api/agui.py` yields `EventType.RUN_ERROR` (around `:341`) on exception. |
| failure C31 | timeout → `RUN_FINISHED` insufficient/timeout | Yes. `agui.py:142-157` `RunFinishedEvent` `result={"outcome": "insufficient", "reason": "timeout"}`. |
| failure C49 | dropped stream refetches thread | Yes. `web/components/Chat.tsx:80-83` `if (terminal === "dropped")` then `fetch(apiUrl(\`/threads/${id}\`))` and `applyReplay`. |
| failure C53 | `RUN_ERROR` message as `--warn` marginalia | Yes. `web/components/renderers.tsx:86` `className="error ui warn"`. |
| idempotency C47 | recents upsert by `threadId`, title 80 chars | Yes. `web/lib/recents.ts:20-23` `message.slice(0, 80)`; filter existing `threadId` then `unshift`. |
| authorization | n/a — v1 has no auth | Policy; nothing in the code for this row to be wrong about. |
| concurrency | n/a — concurrent runs on one thread out of scope | Policy. |
| data lifecycle C37 | pre-feature user dicts map to UserMessages; no migration | Yes. `src/plan_based_researcher/api/replay.py:45-48` `role == "user"` → `UserMessage(id=..., content=...)`. No migration path. |
| dependency failure C30, C31 | same as failure rows | Yes; see C30/C31. |
| state C1 | finalize done appends AIMessage from writer markdown/id | Yes. `src/plan_based_researcher/graph/nodes/finalize.py:32-57` `content = writer_markdown`; `kwargs["id"] = writer_message_id`; `AIMessage`. |
| state C2 | non-done still appends AIMessage with reason + outcome in projection | Yes. `finalize.py:35-48` `content = reason`; `response_metadata: project_turn(state)` includes `outcome`. |
| state C12 | new input on pending `next` starts from START; pending does not run | Yes as product path: `routes.py:52-54` non-resume sets `graph_input = graph.initial_graph_state(query)` (not `None`). No explicit discard-pending helper; relies on LangGraph invoke-with-input. |
| state C40 | `status` interrupted vs idle from snapshot `next` | Yes. `replay.py:15-18` `snapshot_status`: non-empty `next_nodes` → `"interrupted"` else `"idle"`; `routes.py:82` uses it. |
| observability C13 | first frame `RUN_STARTED` with request ids | Yes. `agui.py:115-120` `encoder.encode(RunStartedEvent(..., thread_id=, run_id=))`. |
| observability C19 | `STEP_STARTED` per node name | Yes. `agui.py:205` `EventType.STEP_STARTED`. |
| observability C22 | gate → `ACTIVITY_SNAPSHOT` GATE | Yes. `agui.py:228` `ACTIVITY_SNAPSHOT`. |
| observability C29 | finalize → `RUN_FINISHED` | Yes. `agui.py:331` `RUN_FINISHED`. |
| observability C73 | AGENTS.md `/agent`, `/threads`, `web/`, invariants 1/6/8/9 | Yes. `AGENTS.md` product line names `POST /agent` and `GET /threads/{thread_id}`; commands include `web/`; invariants 1, 6, 8, 9 contain the needles the test locks. |

## Gate

`uv run python -m unittest tests.test_finalize tests.test_writer_stream tests.test_thread_messages tests.test_history_prompts tests.test_writer_thread_evidence tests.test_pending_next tests.test_agent_route tests.test_agui_adapter tests.test_threads_route tests.test_agui_removal tests.test_agui_docs -v` - 62 passed, 0 failed

`npm --prefix web exec -- vitest run` - 29 passed, 0 failed
