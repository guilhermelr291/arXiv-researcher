# RAGAS Writer report `01a0abda-9da0-7712-9373-ab0768e28476`

- scored_at: `2026-09-16T22:03:30Z`
- project: `plan-based-researcher`
- judge: `gpt-4o-mini` + `text-embedding-3-small`

## Query

Which five answer templates can DocuSearch's Template Selection agent assign before retrieval, what BM25 k1 and b does sparse retrieval use, and why does the cross-encoder still keep an RRF term after fusion? (2609.01617)

## Faithfulness: 1.0

| verdict | statement | reason |
| --- | --- | --- |
| 1 | DocuSearch's Template Selection agent assigns each query to one of five templates before retrieval. | The context explicitly states that the Template Selection agent maps queries to one of five types before retrieval. |
| 1 | The five templates assigned by the agent are: `procedure_sop`, `concept_explanation`, `comparison_decision`, `parameter_lookup`, and `troubleshooting_rca`. | The context provides the exact list of five templates that the agent uses. |
| 1 | The agent uses a large language model (LLM) with a deterministic heuristic fallback when the model is uncertain. | The context mentions that the Template Selection agent uses an LLM with a deterministic heuristic as a fallback. |
| 1 | Sparse BM25 retrieval uses the parameters k1 equal to 1.2 and b equal to 0.75. | The context specifies the parameters k1 and b for BM25 scoring. |
| 1 | BM25 retrieves the top kb equal to 40 chunks, but this value is separate from the BM25 scoring parameters. | The context states that BM25 retrieves the top k_b equal to 40 chunks, which is a separate parameter from k1 and b. |
| 1 | After reciprocal rank fusion, the cross-encoder assigns the final score using a specific formula. | The context describes the process of cross-encoder reranking after reciprocal rank fusion. |
| 1 | The formula for the final score includes a larger weight for the cross-encoder to capture fine-grained query–chunk interactions. | The context explains that the cross-encoder score is weighted higher to capture fine-grained interactions. |
| 1 | The RRF term is retained to ensure that the cross-encoder does not completely discard chunks supported by agreement among multiple retrieval signals. | The context states that retaining the RRF component prevents the cross-encoder from discarding evidence supported by multiple signals. |
| 1 | The final ranking combines detailed cross-encoder relevance with evidence that several retrieval methods ranked the same chunk highly. | The context indicates that the final ranking merges results from different retrieval methods, including cross-encoder relevance. |
| 1 | The provided evidence does not establish how the weights 0.65 and 0.35 were selected or tuned. | The context does not provide information on how the weights were selected or tuned, only the formula. |
| 1 | The evidence only reports the formula and the stated reason for retaining the RRF component. | The context confirms that it only discusses the formula and the rationale for retaining the RRF component without further details. |

## Answer relevancy: 0.692506069374188

| generated question | similarity | noncommittal |
| --- | --- | --- |
| What are the five templates assigned by DocuSearch’s Template Selection agent? | 0.6681 | 0 |
| What are the five templates assigned by DocuSearch’s Template Selection agent before retrieval? | 0.7047 | 0 |
| What are the five templates assigned by DocuSearch’s Template Selection agent before retrieval? | 0.7047 | 0 |

## Retrieved contexts

1. Before any retrieval happens, every query $q$ goes through template classification and keyword extraction. The Template Selection agent maps $q$ to one of five types in 𝒯={\mathcal{T}=\{procedure_sop, concept_explanation, comparison_decision, parameter_lookup, troubleshooting_rca$\}$ using an LLM with a deterministic heuristic as fallback when the model is uncertain. Accurate template classification is important because it shapes the structure of the generated answer downstream. The BM25 Keyword agent then extracts domain-specific terms from a curated telecom vocabulary covering standard networking and protocol constructs, supplemented with query-specific expansions. Finally, the Vendor Detection agent combines LLM classification with regex matching to identify vendor references, scoping all retrieval signals to the detected vendor’s documents when applicable.
2. Dense Vector Retrieval. The query $q$ is encoded using BGE-Large-EN-v1.5 [4] to get $\mathbf{q}\in\mathbb{R}^{1024}$. Cosine similarity is computed against all chunk embeddings in Qdrant, returning the top $k_{v}=40$ chunks:   Equation $$ \mathrm{score}_{v}(c)=\frac{\mathbf{q}\cdot\mathbf{e}_{c}}{\\|\mathbf{q}\\|\cdot\\|\mathbf{e}_{c}\\|} $$   BM25 Sparse Retrieval. Extracted keywords query the SQLite FTS5 index. Standard BM25 scoring applies:   Equation $$ \mathrm{score}_{b}(c,Q)=\sum_{q_{i}\in Q}\mathrm{IDF}(q_{i})\cdot\frac{f(q_{i},c)(k_{1}+1)}{f(q_{i},c)+k_{1}(1-b+b\cdot\frac{\|c\|}{avgdl})} $$   with $k_{1}=1.2$, $b=0.75$, where $\|c\|$ is chunk length and $avgdl$ is the average chunk length across $\mathcal{C}$. Top $k_{b}=40$ chunks are retrieved. BM25 is particularly useful here because technical acronyms and model numbers that have no meaningful semantic neighbourhood still produce strong lexical matches. KG Neighbour Expansion. Seed chunks from the vector and BM25 results traverse a KG edge table stored in SQLite, pulling in structurally connected chunks (top $k_{kg}=15$). This catches content that is topically related but lexically and semantically distant — for instance, a configuration snippet that references a procedure described in a different section.
3. The top $k_{r}=12$ fused chunks are reranked with a cross-encoder [8] that jointly encodes the query and each chunk:   Equation $$ s_{\mathrm{final}}(c)=0.65\cdot s_{\mathrm{CE}}(q,c)+0.35\cdot s_{\mathrm{RRF}}(c) $$   The cross-encoder score $s_{\mathrm{CE}}(q,c)$ is weighted higher because it captures fine-grained query-chunk interactions that bi-encoder cosine similarity cannot. Retaining the RRF component prevents the cross-encoder from completely discarding evidence that multiple retrieval signals agreed on.
4. Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search > VII Implementation Details TABLE II: Default Retrieval and Ranking Configuration \| Parameter \| Symbol \| Value \| \| --- \| --- \| --- \| \| Vector top-$k$ \| $k_{v}$ \| 40 \| \| BM25 top-$k$ \| $k_{b}$ \| 40 \| \| KG top-$k$ \| $k_{kg}$ \| 15 \| \| Rerank top-$k$ \| $k_{r}$ \| 12 \| \| Final evidence \| $\|\mathcal{E}\|$ \| 10 \| \| RRF smoothing \| $k$ \| 60 \| \| RRF vector weight \| $w_{v}$ \| 0.50 \| \| RRF BM25 weight \| $w_{b}$ \| 0.35 \| \| RRF KG weight \| $w_{kg}$ \| 0.15 \| \| MMR lambda \| $\lambda$ \| 0.65 \| \| Sufficiency thresh. \| $\tau$ \| 7 \| \| Max neighbour loops \| $L$ \| 2 \| \| Chunk size \| $s$ \| 900 chars \| \| Chunk overlap \| $\delta$ \| 140 chars \| \| LLM temperature \| $T$ \| 0.0 \|
5. Getting accurate, grounded answers out of large enterprise document repositories is a difficult problem. Dense vector retrieval alone frequently performs poorly on queries that mix technical terminology, vendor-specific acronyms, or require reasoning across several non-adjacent sections. DocuSearch was built to address exactly this gap — an offline, multi-agent document intelligence system developed and evaluated in a production telecom network operations environment. Rather than relying on a single retrieval signal, DocuSearch pulls together three complementary sources of evidence: semantic search over a Qdrant vector store using BGE-Large embeddings, BM25 full-text search over an SQLite FTS5 index, and Knowledge Graph neighbour expansion from a structured edge table. These three ranked lists are merged through Reciprocal Rank Fusion (RRF) with signal weights $w_{v}=0.50$, $w_{b}=0.35$, and $w_{kg}=0.15$, giving:   Equation $$ s_{\mathrm{RRF}}(d)\;=\;\sum_{i\in\{v,\,b,\,kg\}}\frac{w_{i}}{k+r_{i}(d)},\quad k=60 $$   where $r_{i}(d)$ is the rank of chunk $d$ in signal list $i$. A cross-encoder then reranks the fused list, and Maximal Marginal Relevance (MMR) with $\lambda=0.65$ prunes it for both relevance and diversity. What makes DocuSearch distinctive is a per-chunk evaluation loop that treats each selected chunk as its own mini-retrieval problem: an LLM decides whether the chunk needs additional neighbouring context, whether it is actually sufficient to answer the query, and whether the answer it generates is genuinely grounded in the retrieved text. Ungrounded answers are not returned; the system falls back to a multi-chunk merge instead. On an internal telecom document corpus, DocuSearch reaches Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\%$ — gains of $15$, $16$, and $18.4$ percentage points over a dense-only RAG baseline.
6. DocuSearch tackles the four sub-problems through a layered multi-agent pipeline split into two phases, illustrated in Figure[1](#S4.F1 "Figure 1 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") and Figure[2](#S4.F2 "Figure 2 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search"). Pre-MMR Phase. When a query $q$ arrives, a Template Selection agent first classifies it into one of five answer templates $\mathcal{T}$. A BM25 Keyword agent then pulls out domain-specific terms, and a Vendor Detection agent optionally narrows all three retrieval signals to a specific vendor namespace $v\in\mathcal{V}$. Dense vector search, BM25 full-text search, and KG neighbour expansion run in parallel, and their ranked results are merged by weighted RRF, reranked by a cross-encoder, and filtered by MMR to produce the evidence set $\mathcal{E}$. Post-MMR Phase. Each chunk $c\in\mathcal{E}$ is evaluated individually by four sequential LLM-driven nodes: context need detection, sufficiency scoring against a threshold $\tau=7$, answer generation, and groundedness verification. The first chunk that clears all four checks produces the final answer. When no individual chunk passes, a fallback merges all expanded contexts, generates a consolidated answer, and runs one final groundedness check before returning anything to the user.
7. In telecom network operations centres, engineers frequently must search through hundreds of vendor manuals for a specific configuration procedure under time pressure. These environments accumulate vast repositories of technical documentation — SOPs, RCA reports, audit records, multi-vendor configuration guides — and the gap between what keyword search surfaces and what an engineer actually needs can be substantial. Conventional search returns too many loosely related hits. Standard single-signal Retrieval-Augmented Generation (RAG)[1], while an improvement, still struggles with specialised technical vocabulary, fragmented document chunks, and the reality that relevant information is often spread across non-adjacent sections from different vendors. This paper describes DocuSearch, a hybrid multi-agent RAG system designed and deployed within a real telecom network operations environment. Three limitations motivated its design: signal incompleteness, where a single retrieval function misses chunks that are relevant in semantic, lexical, and structural senses simultaneously; context fragmentation, where a retrieved chunk is only half of a procedure; and unverified grounding, where a generated answer sounds plausible but is not actually supported by the retrieved evidence. DocuSearch addresses all three by combining dense vector retrieval, BM25 sparse retrieval [2], and Knowledge Graph expansion[9] into a single fused ranking via Reciprocal Rank Fusion (RRF) [7], followed by cross-encoder reranking, Maximal Marginal Relevance (MMR) selection [3], and — the primary novel contribution — a per-chunk agentic evaluation loop that verifies context sufficiency and answer grounding before committing to a response. The pipeline runs entirely on locally hosted models and is orchestrated via LangGraph [5], exposed through an interactive Dash web application with vendor-scoped retrieval, session document upload, and integrated Knowledge and Learning Hub modules. The main contributions are as follows:   * •   A three-signal hybrid retrieval architecture fused via weighted RRF with signal weights $w_{v}{=}0.50,\;w_{b}{=}0.35,\;w_{kg}{=}0.15$. * •   A per-chunk agentic evaluation loop with LLM-driven context need detection, sufficiency scoring, and groundedness verification. * •   Vendor-aware retrieval scoping for multi-vendor
8. The three ranked lists are fused following [7]:   Equation $$ s_{\mathrm{RRF}}(c)=\sum_{i\in\{v,\,b,\,kg\}}\frac{w_{i}}{k+r_{i}(c)} $$   Equation $$ w_{v}{=}0.50,\quad w_{b}{=}0.35,\quad w_{kg}{=}0.15,\quad k{=}60 $$   Chunks that surface in multiple lists receive additive contributions, naturally elevating content that is relevant across different notions of similarity. The smoothing constant $k=60$ prevents very high-ranked items from dominating the fused score entirely.

## Response

## 1. Pre-retrieval answer templates

Before retrieval, DocuSearch’s Template Selection agent assigns each query to one of these five templates: [1]

1. `procedure_sop`
2. `concept_explanation`
3. `comparison_decision`
4. `parameter_lookup`
5. `troubleshooting_rca`

The agent uses an LLM, with a deterministic heuristic fallback when the model is uncertain. [1]

## 2. BM25 parameters

Sparse BM25 retrieval uses:

- \(k_1 = 1.2\)
- \(b = 0.75\) [2]

The paper also states that BM25 retrieves the top \(k_b=40\) chunks, but that value is separate from the BM25 scoring parameters. [2]

## 3. Why retain an RRF term after fusion?

After reciprocal rank fusion, the cross-encoder assigns the final score as:

\[
s_{\mathrm{final}}(c)
=
0.65\,s_{\mathrm{CE}}(q,c)
+
0.35\,s_{\mathrm{RRF}}(c).
\]

The cross-encoder receives the larger weight because it captures fine-grained query–chunk interactions. The RRF term is retained so that the cross-encoder cannot completely discard chunks supported by agreement among multiple retrieval signals. [3] In other words, the final ranking combines detailed cross-encoder relevance with evidence that several retrieval methods ranked the same chunk highly. [3][8]

The provided evidence does not establish how the weights \(0.65\) and \(0.35\) were selected or tuned; it only reports the formula and the stated reason for retaining the RRF component. [3]
