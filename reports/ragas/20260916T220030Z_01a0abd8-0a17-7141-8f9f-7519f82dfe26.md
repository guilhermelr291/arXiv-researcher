# RAGAS Writer report `01a0abd8-0a17-7141-8f9f-7519f82dfe26`

- scored_at: `2026-09-16T22:00:30Z`
- project: `plan-based-researcher`
- judge: `gpt-4o-mini` + `text-embedding-3-small`

## Query

What is DocuSearch's mean end-to-end latency, which phase dominates that latency, and what future work does the paper propose to reduce it? (2609.01617)

## Faithfulness: 1.0

| verdict | statement | reason |
| --- | --- | --- |
| 1 | DocuSearch has a mean end-to-end latency of 12.6 seconds across 200 queries. | The context explicitly states that the mean end-to-end latency across 200 queries is 12.6 seconds. |
| 1 | The dominant phase of latency is per-chunk evaluation, which accounts for 8.4 seconds. | The context mentions that the per-chunk evaluation phase accounts for 8.4 seconds of the total latency. |
| 1 | The paper identifies sequential LLM calls as the main bottleneck in the per-chunk evaluation phase. | The context states that sequential LLM calls in the per-chunk evaluation phase are the main latency bottleneck. |
| 1 | Retrieval itself takes under one second. | The context indicates that retrieval finishes in under one second. |
| 1 | The proposed method for latency reduction is to parallelize chunk evaluation across inference threads. | The context suggests that parallelising chunk evaluation across inference threads is a natural first step for reducing latency. |
| 1 | The paper identifies reducing evaluation latency as an explicit future-work target. | The context states that reducing evaluation latency is considered an obvious target for future work. |
| 1 | No contradictory latency figures or proposals are provided in the evidence. | The context does not provide any contradictory figures or proposals regarding latency, supporting the statements made. |

## Answer relevancy: 0.5140277303117023

| generated question | similarity | noncommittal |
| --- | --- | --- |
| What is the mean end-to-end latency across 200 queries? | 0.5140 | 0 |
| What is the mean end-to-end latency across 200 queries? | 0.5140 | 0 |
| What is the mean end-to-end latency across 200 queries? | 0.5140 | 0 |

## Retrieved contexts

1. Mean end-to-end latency across $200$ queries is $12.6$ seconds, with the per-chunk evaluation phase accounting for $8.4$ seconds of that. Retrieval itself finishes in under one second thanks to Qdrant ANN search and SQLite FTS5. For a document intelligence application where an engineer is waiting for a reliable answer rather than streaming tokens, this trade-off is considered acceptable. That said, reducing evaluation latency is an obvious target for future work.
2. The sequential LLM calls in the per-chunk evaluation phase are the main latency bottleneck, currently averaging $8.4$ seconds. Parallelising chunk evaluation across inference threads is a natural first step. The RRF signal weights were determined empirically on telecom documentation and may not transfer well to other domains; learning them from query-relevance feedback via a differentiable ranking objective would make the system more broadly applicable. There is also potential in using named entity recognition to populate the KG with typed entity-relation triples, which would strengthen multi-hop query handling. Finally, extending DocuSearch to multimodal retrieval — covering embedded diagrams and specification tables that are pervasive in vendor documentation — is a compelling direction worth pursuing. Reinforcement Learning for Adaptive Retrieval. One promising future direction is treating DocuSearch’s multi-step retrieval workflow as a sequential decision problem. The intuition comes from Karpathy’s interactive GridWorld temporal-difference learning demo[11], where an agent learns to navigate a grid by trial-and-error using TD updates — without any hard-coded path rules. The analogy to the pipeline is direct: just as that agent discovers which moves lead to reward by accumulating Q-values over many episodes, a retrieval agent in DocuSearch could learn when to invoke BM25, when to expand KG neighbours, and when to stop — adapting dynamically to each incoming query rather than applying the same fixed configuration every time. Formally, each query-answer interaction can be modelled as an episode in a Markov Decision Process $\mathcal{M}=(\mathcal{S},\mathcal{A},\mathcal{P},\mathcal{R},\gamma)$. The state $s\in\mathcal{S}$ encodes the current query intent, the set of chunks retrieved so far, reranker confidence scores, KG expansion status, and evidence coverage. The action space $\mathcal{A}$ includes discrete choices such as triggering a BM25 search, expanding KG neighbours by one hop, tightening the cross-encoder top-$k$, fetching a neighbouring chunk, or terminating retrieval and proceeding to answer generation. The reward $\mathcal{R}$ is shaped by answer groundedness (which is already measured at inference time), engineer satisfaction feedback collected through

## Response

- **Mean end-to-end latency:** **12.6 seconds** across 200 queries.[1]
- **Dominant phase:** **Per-chunk evaluation**, which accounts for **8.4 seconds**; the paper identifies its sequential LLM calls as the main bottleneck. Retrieval itself takes under one second.[1][2]
- **Proposed latency reduction:** **Parallelise chunk evaluation across inference threads**. The paper also identifies reducing evaluation latency as an explicit future-work target.[1][2]

No contradictory latency figures or proposals are provided in the evidence.
