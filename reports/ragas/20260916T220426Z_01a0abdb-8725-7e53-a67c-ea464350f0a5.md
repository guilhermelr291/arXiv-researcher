# RAGAS Writer report `01a0abdb-8725-7e53-a67c-ea464350f0a5`

- scored_at: `2026-09-16T22:04:26Z`
- project: `plan-based-researcher`
- judge: `gpt-4o-mini` + `text-embedding-3-small`

## Query

What four core sub-problems does the paper identify as challenges standard RAG pipelines fail to handle, what BM25 weight and smoothing constant does DocuSearch's RRF fusion use, what chunk size and overlap does it use for ingestion, and what sufficiency score threshold must a chunk clear before answer generation? (2609.01617)

## Faithfulness: 0.9090909090909091

| verdict | statement | reason |
| --- | --- | --- |
| 1 | The paper identifies four core sub-problems that standard RAG pipelines fail to handle. | The context explicitly states that there are four concrete sub-problems that standard pipelines fail to handle. |
| 1 | The first sub-problem is signal incompleteness, where no single retrieval function captures semantic, lexical, and structural relevance simultaneously. | The context describes signal incompleteness as the first sub-problem, detailing that no single retrieval function can capture all relevant chunks. |
| 1 | The second sub-problem is chunk fragmentation, where an isolated chunk may be incomplete when the relevant procedure spans neighboring chunks. | The context clearly states that chunk fragmentation is the second sub-problem and explains the issue of isolated chunks being incomplete. |
| 1 | The third sub-problem is redundancy in evidence, where naïve top-k retrieval can return near-duplicate chunks and waste context capacity. | The context identifies redundancy in evidence as the third sub-problem and describes how naive top-k selection can lead to this issue. |
| 1 | The fourth sub-problem is unverified grounding, where standard RAG may generate an answer without checking whether it is supported by the retrieved evidence. | The context explicitly mentions unverified grounding as the fourth sub-problem and explains the risks associated with it. |
| 1 | DocuSearch uses a BM25 RRF weight of 0.35. | The context provides specific details about the RRF weights used in DocuSearch, including the BM25 weight of 0.35. |
| 1 | DocuSearch uses an RRF smoothing constant of 60. | The context explicitly states that the RRF smoothing constant is set to 60. |
| 1 | DocuSearch uses an ingestion chunk size of 900 characters. | The context mentions that documents are chunked with a size of 900 characters. |
| 1 | DocuSearch uses a chunk overlap of 140 characters. | The context specifies that the chunk overlap is set to 140 characters. |
| 1 | A chunk must receive a sufficiency score of 7 or higher on a 1–10 scale before answer generation is attempted. | The context clearly states that a sufficiency score of 7 or higher is required before generating an answer. |
| 0 | No requested detail is missing or ambiguous in the provided evidence. | The context does not provide any information to support this statement, as it discusses the challenges of retrieval and grounding. |

## Answer relevancy: 0.5947138036539302

| generated question | similarity | noncommittal |
| --- | --- | --- |
| What are the four core sub-problems that standard RAG pipelines fail to handle? | 0.5947 | 0 |
| What are the four core sub-problems that standard RAG pipelines fail to handle? | 0.5947 | 0 |
| What are the four core sub-problems that standard RAG pipelines fail to handle? | 0.5947 | 0 |

## Retrieved contexts

1. Let $\mathcal{D}=\{d_{1},d_{2},\ldots,d_{N}\}$ be a large heterogeneous corpus of enterprise documents, where each document $d_{i}$ belongs to one or more vendor namespaces $\mathcal{V}=\{v_{1},v_{2},\ldots,v_{M}\}$. Each document is pre-processed into overlapping text chunks:   Equation $$ \mathcal{C}=\bigcup_{i=1}^{N}\bigl\{c_{i,1},\,c_{i,2},\,\ldots,\,c_{i,k_{i}}\bigr\} $$   where $c_{i,j}$ is the $j$-th chunk of $d_{i}$ and $k_{i}$ is the total chunk count for that document. Given a natural language query $q$, the goal is to retrieve a subset $\mathcal{E}\subset\mathcal{C}$ and generate an answer $a$ such that:   Equation $$ a=\mathcal{G}\!\left(q,\;\mathcal{E}\right) $$   where $\mathcal{G}$ synthesises the answer strictly from retrieved evidence, without hallucinating content that does not appear in $\mathcal{E}$. This deceptively simple formulation hides four concrete sub-problems that standard pipelines fail to handle:   1. 1.   Signal incompleteness. No single retrieval function $f:q\rightarrow\mathcal{C}$ can capture all relevant chunks, because relevance spans semantic, lexical, and structural dimensions at the same time. 2. 2.   Chunk fragmentation. A chunk $c_{i,j}$ retrieved in isolation is often incomplete when the relevant procedure spans $c_{i,j-1}$ through $c_{i,j+1}$. 3. 3.   Redundancy in evidence. Naive top-$k$ selection tends to return clusters of near-duplicate chunks, squandering context window capacity on repeated information. 4. 4.   Unverified grounding. Standard RAG hands retrieved chunks to $\mathcal{G}$ without ever checking whether the generated answer $a$ is genuinely supported by $\mathcal{E}$, which is a direct path to confident but wrong responses.   DocuSearch addresses all four within a unified pipeline that runs entirely on locally hosted models with no dependency on external API services.
2. Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search > VII Implementation Details TABLE II: Default Retrieval and Ranking Configuration \| Parameter \| Symbol \| Value \| \| --- \| --- \| --- \| \| Vector top-$k$ \| $k_{v}$ \| 40 \| \| BM25 top-$k$ \| $k_{b}$ \| 40 \| \| KG top-$k$ \| $k_{kg}$ \| 15 \| \| Rerank top-$k$ \| $k_{r}$ \| 12 \| \| Final evidence \| $\|\mathcal{E}\|$ \| 10 \| \| RRF smoothing \| $k$ \| 60 \| \| RRF vector weight \| $w_{v}$ \| 0.50 \| \| RRF BM25 weight \| $w_{b}$ \| 0.35 \| \| RRF KG weight \| $w_{kg}$ \| 0.15 \| \| MMR lambda \| $\lambda$ \| 0.65 \| \| Sufficiency thresh. \| $\tau$ \| 7 \| \| Max neighbour loops \| $L$ \| 2 \| \| Chunk size \| $s$ \| 900 chars \| \| Chunk overlap \| $\delta$ \| 140 chars \| \| LLM temperature \| $T$ \| 0.0 \|
3. DocuSearch is implemented entirely in Python3 and served from a single application. Table[I](#S7.T1 "TABLE I ‣ VII Implementation Details ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") summarises the core technology stack.   TABLE I: DocuSearch Core Technology Stack \| Component \| Technology \| \| --- \| --- \| \| Vector Store \| Qdrant (self-hosted) \| \| Embedding \| BGE-Large-EN-v1.5 ($d{=}1024$, CUDA) \| \| Reranker \| Cross-Encoder (CUDA) \| \| FTS Index \| SQLite FTS5 \| \| Metadata Store \| SQLite \| \| LLM Inference \| Local REST or vLLM \| \| LLM Model \| Mistral [10] \| \| Pipeline Graph \| LangGraph \| \| UI Framework \| Plotly Dash \| \| Doc Parsing \| Docling, PyMuPDF, python-docx, openpyxl \| \| Auto Ingest \| watchdog directory monitor \|    Documents reach the system through three paths: server-side path submission, browser upload via Dash dcc.Upload, or automatic detection through a watchdog directory monitor. All formats are parsed and chunked with $s=900$ characters and $\delta=140$ characters overlap. These values were settled on after experimenting with several chunk sizes; smaller chunks hurt sufficiency scoring while larger ones degraded retrieval precision. Table[II](#S7.T2 "TABLE II ‣ VII Implementation Details ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") summarises the full retrieval configuration used across all experiments.   TABLE II: Default Retrieval and Ranking Configuration   Session-only uploads are also supported: documents uploaded during a session are embedded in memory using a local NumPy array (max $90{,}000$ chars, top-$k_{u}=8$) without being written to the persistent knowledge base. All LLM calls use $T=0.0$ to keep outputs deterministic across repeated queries, which is important for reproducibility in an operational setting.
4. Each $c\in\mathcal{E}$ passes four sequential LLM-driven checks, as shown in Figure[2](#S4.F2 "Figure 2 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search"):   1. 1.   Context Need Detection. The LLM assesses whether $c$ is a fragment that requires neighbours $c_{\mathrm{prev}}$ or $c_{\mathrm{next}}$ to make sense. If so, neighbours are fetched and appended, for up to $L=2$ iterations. 2. 2.   Sufficiency Scoring. The expanded chunk $\tilde{c}$ proceeds only if the LLM assigns a sufficiency score $\sigma(\tilde{c},q)\geq\tau=7$ on a $1$–$10$ scale. Chunks that do not clear this bar are skipped immediately. 3. 3.   Answer Generation. Given $\tilde{c}$, the query $q$, and the selected template $t^{\*}$, the LLM generates candidate answer $\hat{a}$ at temperature $T=0.0$ for reproducibility. 4. 4.   Groundedness Verification. A separate LLM call checks whether $\hat{a}$ is fully supported by $\tilde{c}$. If it is, $a$ is set to $\hat{a}$ and the pipeline terminates (method=per_chunk).   When no chunk clears all four checks, the fallback merges all expanded contexts into $\tilde{\mathcal{E}}$, generates a consolidated answer (method=fallback_merge), and runs one final groundedness check before anything is returned to the user.
5. DocuSearch tackles the four sub-problems through a layered multi-agent pipeline split into two phases, illustrated in Figure[1](#S4.F1 "Figure 1 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") and Figure[2](#S4.F2 "Figure 2 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search"). Pre-MMR Phase. When a query $q$ arrives, a Template Selection agent first classifies it into one of five answer templates $\mathcal{T}$. A BM25 Keyword agent then pulls out domain-specific terms, and a Vendor Detection agent optionally narrows all three retrieval signals to a specific vendor namespace $v\in\mathcal{V}$. Dense vector search, BM25 full-text search, and KG neighbour expansion run in parallel, and their ranked results are merged by weighted RRF, reranked by a cross-encoder, and filtered by MMR to produce the evidence set $\mathcal{E}$. Post-MMR Phase. Each chunk $c\in\mathcal{E}$ is evaluated individually by four sequential LLM-driven nodes: context need detection, sufficiency scoring against a threshold $\tau=7$, answer generation, and groundedness verification. The first chunk that clears all four checks produces the final answer. When no individual chunk passes, a fallback merges all expanded contexts, generates a consolidated answer, and runs one final groundedness check before returning anything to the user.
6. Getting accurate, grounded answers out of large enterprise document repositories is a difficult problem. Dense vector retrieval alone frequently performs poorly on queries that mix technical terminology, vendor-specific acronyms, or require reasoning across several non-adjacent sections. DocuSearch was built to address exactly this gap — an offline, multi-agent document intelligence system developed and evaluated in a production telecom network operations environment. Rather than relying on a single retrieval signal, DocuSearch pulls together three complementary sources of evidence: semantic search over a Qdrant vector store using BGE-Large embeddings, BM25 full-text search over an SQLite FTS5 index, and Knowledge Graph neighbour expansion from a structured edge table. These three ranked lists are merged through Reciprocal Rank Fusion (RRF) with signal weights $w_{v}=0.50$, $w_{b}=0.35$, and $w_{kg}=0.15$, giving:   Equation $$ s_{\mathrm{RRF}}(d)\;=\;\sum_{i\in\{v,\,b,\,kg\}}\frac{w_{i}}{k+r_{i}(d)},\quad k=60 $$   where $r_{i}(d)$ is the rank of chunk $d$ in signal list $i$. A cross-encoder then reranks the fused list, and Maximal Marginal Relevance (MMR) with $\lambda=0.65$ prunes it for both relevance and diversity. What makes DocuSearch distinctive is a per-chunk evaluation loop that treats each selected chunk as its own mini-retrieval problem: an LLM decides whether the chunk needs additional neighbouring context, whether it is actually sufficient to answer the query, and whether the answer it generates is genuinely grounded in the retrieved text. Ungrounded answers are not returned; the system falls back to a multi-chunk merge instead. On an internal telecom document corpus, DocuSearch reaches Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\%$ — gains of $15$, $16$, and $18.4$ percentage points over a dense-only RAG baseline.
7. Five aspects of DocuSearch distinguish it from existing RAG systems. Per-Chunk Agentic Evaluation Loop. Most RAG pipelines concatenate all retrieved chunks and generate one answer in a single forward pass. DocuSearch instead evaluates each $c\in\mathcal{E}$ independently through four sequential checks. A chunk must score $\sigma(\tilde{c},q)\geq\tau=7$ before generation is even attempted, and the returned answer is explicitly verified as grounded before being committed. This eliminates a whole class of hallucinations that arise when an LLM is asked to synthesise an answer from a mixed bag of partially relevant context. Weighted Three-Signal RRF Fusion. Most hybrid RAG systems stop at two signals — typically dense plus sparse. A third, KG neighbour expansion, is added as an explicit signal in the RRF formula, with empirically tuned weights $w_{v}=0.50$, $w_{b}=0.35$, $w_{kg}=0.15$. The KG signal’s contribution is modest but consistent: it reliably surfaces structural neighbours that neither cosine similarity nor BM25 would rank highly on their own. Dynamic Neighbour Context Expansion. Rather than using larger fixed chunk sizes at indexing time (which reduces retrieval precision), DocuSearch detects fragmentation at query time. When a retrieved chunk is identified as a fragment, up to $L=2$ levels of neighbouring chunks are fetched from the metadata database on the fly, recovering coherent multi-step procedures without any pre-processing trade-offs. Vendor-Aware Retrieval Scoping. A vendor registry is built from the document metadata that maps each $v\in\mathcal{V}$ to its set of document identifiers. When the Vendor Detection agent identifies a vendor reference in the query, all three retrieval signals are scoped to that vendor’s documents. The noise reduction for vendor-specific configuration and troubleshooting queries is substantial. LangGraph-Orchestrated Agentic Pipeline. The entire pipeline runs as a stateful LangGraph DAG [5] with five explicitly defined nodes. Every intermediate state is observable, serialisable, and independently replaceable — which turned out to be practically important during development, since individual components could be swapped without disturbing anything upstream or downstream.
8. The three ranked lists are fused following [7]:   Equation $$ s_{\mathrm{RRF}}(c)=\sum_{i\in\{v,\,b,\,kg\}}\frac{w_{i}}{k+r_{i}(c)} $$   Equation $$ w_{v}{=}0.50,\quad w_{b}{=}0.35,\quad w_{kg}{=}0.15,\quad k{=}60 $$   Chunks that surface in multiple lists receive additive contributions, naturally elevating content that is relevant across different notions of similarity. The smoothing constant $k=60$ prevents very high-ranked items from dominating the fused score entirely.
9. The system is organised across six functional tiers spanning the two phases shown in Figures[1](#S4.F1 "Figure 1 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") and[2](#S4.F2 "Figure 2 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search").   Figure 1: Pre-MMR Phase ( run_supervisor ): Step 1 — Template Selection (LLM call); Step 2 — BM25 Keyword Extraction (LLM + Rule Based); Step 2b — Vendor Detection (LLM call); Steps 3/4/5 — Parallel Vector Search, BM25 Retrieval, KG Expansion; Step 5b — RRF Fusion ( w v = 0.50 , w b = 0.35 , w k  g = 0.15 w_{v}{=}0.50,\,w_{b}{=}0.35,\,w_{kg}{=}0.15 ); Step 6 — Cross-Encoder Reranking; Step 7 — MMR Selection ( λ = 0.65 \lambda{=}0.65 , relevance + diversity). Output: ordered MMR chunks passed to Post-MMR phase. Figure 2: Post-MMR Phase ( run_per_chunk_evaluation ): Per-chunk loop — LLM evaluates current chunk; if more context needed, neighbouring chunks are fetched (green path); sufficiency check gates answer generation; groundedness LLM verifies the generated answer; if grounded, pipeline terminates ( method=per_chunk ); otherwise retries next MMR chunk. When all MMR chunks are exhausted, fallback merges all expanded contexts and returns a consolidated answer ( method=fallback_merge ).   The Document Ingestion Layer handles parsing and chunking, producing overlapping segments ($s=900$ chars, $\delta=140$ chars) stored in both Qdrant[6] and SQLite FTS5. The Multi-Signal Retrieval Layer runs three parallel agents ($k_{v}{=}40$, $k_{b}{=}40$, $k_{kg}{=}15$), each

## Response

## Four core sub-problems

The paper says standard RAG pipelines fail to handle:

1. **Signal incompleteness:** no single retrieval function captures semantic, lexical, and structural relevance simultaneously. [1]
2. **Chunk fragmentation:** an isolated chunk may be incomplete when the relevant procedure spans neighboring chunks. [1]
3. **Redundancy in evidence:** naïve top-\(k\) retrieval can return near-duplicate chunks and waste context capacity. [1]
4. **Unverified grounding:** standard RAG may generate an answer without checking whether it is supported by the retrieved evidence. [1]

## DocuSearch configuration

- **BM25 RRF weight:** \(w_b = 0.35\). [2]
- **RRF smoothing constant:** \(k = 60\). [2]
- **Ingestion chunk size:** \(900\) characters. [3]
- **Chunk overlap:** \(140\) characters. [3]
- **Sufficiency-score gate:** a chunk must receive \(\sigma(\tilde c,q) \ge 7\) on a \(1\)–\(10\) scale before answer generation is attempted. [4]

No requested detail is missing or ambiguous in the provided evidence.
