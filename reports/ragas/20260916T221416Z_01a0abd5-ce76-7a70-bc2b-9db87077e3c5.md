# RAGAS Writer report `01a0abd5-ce76-7a70-bc2b-9db87077e3c5`

- scored_at: `2026-09-16T22:14:16Z`
- project: `plan-based-researcher`
- judge: `gpt-4o-mini` + `text-embedding-3-small`

## Query

What grounding and hallucination rates does DocuSearch achieve compared to a single-pass RAG baseline? (2609.01617)

## Faithfulness: 0.8260869565217391

| verdict | statement | reason |
| --- | --- | --- |
| 1 | The results of the comparison are presented in a table format. | The context explicitly mentions that the results are presented in a table format. |
| 1 | The table includes the grounding rate, hallucination rate, and fallback for two systems: the single-pass RAG baseline and DocuSearch. | The context describes a table that compares the grounding rate, hallucination rate, and fallback for both systems. |
| 1 | The grounding rate for the single-pass RAG baseline is 71.2%. | The context provides the grounding rate for the single-pass RAG baseline as 71.2%. |
| 1 | The hallucination rate for the single-pass RAG baseline is 21.4%. | The context states the hallucination rate for the single-pass RAG baseline as 21.4%. |
| 1 | The grounding rate for DocuSearch is 89.6%. | The context explicitly mentions that DocuSearch achieves a grounding rate of 89.6%. |
| 1 | The hallucination rate for DocuSearch is 6.8%. | The context states that the hallucination rate for DocuSearch is 6.8%. |
| 1 | The fallback rate for DocuSearch is 3.6%. | The context mentions that the fallback rate for DocuSearch is 3.6%. |
| 1 | The grounding rate measures whether the generated answer is supported by the retrieved context. | The context explains that the grounding rate indicates if the generated answer is supported by the retrieved context. |
| 1 | DocuSearch’s verifier accepts an answer only when the answer is fully supported by the expanded chunk. | The context states that DocuSearch's verifier checks if the answer is fully supported by the expanded chunk. |
| 1 | The hallucination rate is the reported proportion of answers classified as hallucinated rather than grounded. | The context defines the hallucination rate as the proportion of answers classified as hallucinated. |
| 1 | DocuSearch improves grounding by 18.4 percentage points compared to the single-pass RAG baseline. | The context states that DocuSearch achieves an 18.4 percentage point improvement in grounding over the single-pass RAG baseline. |
| 1 | The grounding rate improves from 71.2% to 89.6% with DocuSearch. | The context explicitly states that the grounding rate improves from 71.2% to 89.6% with DocuSearch. |
| 1 | DocuSearch reduces hallucinations by 14.6 percentage points compared to the single-pass RAG baseline. | The context mentions that DocuSearch reduces the hallucination rate by 14.6 percentage points compared to the single-pass RAG baseline. |
| 1 | The hallucination rate reduces from 21.4% to 6.8% with DocuSearch. | The context states that the hallucination rate reduces from 21.4% to 6.8% with DocuSearch. |
| 1 | The remaining 3.6% of DocuSearch cases use a fallback that merges the expanded contexts before generating and checking a consolidated answer. | The context explains that 3.6% of DocuSearch cases use a fallback that merges expanded contexts. |
| 0 | The comparison is not completely transparent. | The context does not provide sufficient information to determine the transparency of the comparison. |
| 1 | The baseline row has no reported fallback category. | The context does not mention any fallback category for the baseline row. |
| 1 | DocuSearch’s categories sum to 100% only when fallback cases are included. | The context indicates that the categories for DocuSearch sum to 100% when including fallback cases. |
| 1 | The paper states that fallback cases still produce grounded answers. | The context mentions that fallback cases still produce grounded answers. |
| 1 | The table lists fallback separately from the 89.6% grounded category. | The context states that the table lists fallback separately from the grounded category. |
| 0 | The excerpt does not clarify whether the 89.6% grounding rate excludes or includes grounded fallback answers. | The context does not provide clarity on whether the 89.6% grounding rate includes fallback answers. |
| 0 | The reported rates should be read as table-level results. | The context does not specify how the reported rates should be interpreted. |
| 0 | The reported rates are not necessarily directly comparable or mutually exhaustive outcome categories. | The context does not provide information on the comparability or exhaustiveness of the reported rates. |

## Answer relevancy: 0.0

| generated question | similarity | noncommittal |
| --- | --- | --- |
| What are the grounding and hallucination rates for DocuSearch compared to the single-pass RAG baseline? | 0.9575 | 1 |
| What are the grounding and hallucination rates for DocuSearch compared to the single-pass RAG baseline? | 0.9575 | 1 |
| What are the grounding and hallucination rates for DocuSearch compared to the single-pass RAG baseline? | 0.9575 | 1 |

All generated questions were marked noncommittal.

## Retrieved contexts

1. Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search > VIII Results and Analysis > VIII-B Answer Grounding TABLE IV: Answer Grounding and Hallucination Rates \| System \| Grounded \| Hallucinated \| Fallback \| \| --- \| --- \| --- \| --- \| \| Single-pass RAG \| 71.2% \| 21.4% \| — \| \| DocuSearch \| 89.6% \| 6.8% \| 3.6% \|
2. Table[IV](#S8.T4 "TABLE IV ‣ VIII-B Answer Grounding ‣ VIII Results and Analysis ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") compares the per-chunk evaluation loop against a conventional single-pass RAG baseline.   TABLE IV: Answer Grounding and Hallucination Rates   DocuSearch achieves a grounding rate of $89.6\%$, an $18.4$ percentage point improvement over single-pass RAG, and cuts the hallucination rate from $21.4\%$ to $6.8\%$. The remaining $3.6\%$ of queries hit the fallback path, where all expanded contexts are merged — these still produce grounded answers, just through the consolidated route rather than the per-chunk route.
3. This paper presented DocuSearch, a hybrid multi-agent RAG system built for enterprise document intelligence in multi-vendor telecom environments. The system was designed around three failure modes observed in production: signal incompleteness, context fragmentation, and unverified answer grounding. The two-phase pipeline — hybrid retrieval fusion followed by per-chunk agentic evaluation — directly addresses all three. Results show Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\%$, with hallucinations dropping to $6.8\%$. The ablation confirms that groundedness verification and sufficiency scoring drive the largest improvements, though no single component is redundant. A key practical finding is that production-quality document intelligence is achievable entirely on locally hosted models. DocuSearch requires no external API calls, which matters a great deal in telecom operations environments where sensitive network configuration data cannot leave the organisation’s infrastructure.
4. Getting accurate, grounded answers out of large enterprise document repositories is a difficult problem. Dense vector retrieval alone frequently performs poorly on queries that mix technical terminology, vendor-specific acronyms, or require reasoning across several non-adjacent sections. DocuSearch was built to address exactly this gap — an offline, multi-agent document intelligence system developed and evaluated in a production telecom network operations environment. Rather than relying on a single retrieval signal, DocuSearch pulls together three complementary sources of evidence: semantic search over a Qdrant vector store using BGE-Large embeddings, BM25 full-text search over an SQLite FTS5 index, and Knowledge Graph neighbour expansion from a structured edge table. These three ranked lists are merged through Reciprocal Rank Fusion (RRF) with signal weights $w_{v}=0.50$, $w_{b}=0.35$, and $w_{kg}=0.15$, giving:   Equation $$ s_{\mathrm{RRF}}(d)\;=\;\sum_{i\in\{v,\,b,\,kg\}}\frac{w_{i}}{k+r_{i}(d)},\quad k=60 $$   where $r_{i}(d)$ is the rank of chunk $d$ in signal list $i$. A cross-encoder then reranks the fused list, and Maximal Marginal Relevance (MMR) with $\lambda=0.65$ prunes it for both relevance and diversity. What makes DocuSearch distinctive is a per-chunk evaluation loop that treats each selected chunk as its own mini-retrieval problem: an LLM decides whether the chunk needs additional neighbouring context, whether it is actually sufficient to answer the query, and whether the answer it generates is genuinely grounded in the retrieved text. Ungrounded answers are not returned; the system falls back to a multi-chunk merge instead. On an internal telecom document corpus, DocuSearch reaches Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\%$ — gains of $15$, $16$, and $18.4$ percentage points over a dense-only RAG baseline.
5. Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search > VIII Results and Analysis > VIII-D Ablation Study TABLE V: Ablation Study: Grounding Rate \| Configuration \| Grounding Rate \| \| --- \| --- \| \| Full DocuSearch \| 89.6% \| \| Without groundedness check \| 74.3% \| \| Without sufficiency scoring \| 78.1% \| \| Without neighbour expansion \| 82.4% \| \| Without cross-encoder reranking \| 83.7% \| \| Without KG expansion \| 85.2% \| \| Without vendor scoping \| 86.1% \|
6. Each $c\in\mathcal{E}$ passes four sequential LLM-driven checks, as shown in Figure[2](#S4.F2 "Figure 2 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search"):   1. 1.   Context Need Detection. The LLM assesses whether $c$ is a fragment that requires neighbours $c_{\mathrm{prev}}$ or $c_{\mathrm{next}}$ to make sense. If so, neighbours are fetched and appended, for up to $L=2$ iterations. 2. 2.   Sufficiency Scoring. The expanded chunk $\tilde{c}$ proceeds only if the LLM assigns a sufficiency score $\sigma(\tilde{c},q)\geq\tau=7$ on a $1$–$10$ scale. Chunks that do not clear this bar are skipped immediately. 3. 3.   Answer Generation. Given $\tilde{c}$, the query $q$, and the selected template $t^{\*}$, the LLM generates candidate answer $\hat{a}$ at temperature $T=0.0$ for reproducibility. 4. 4.   Groundedness Verification. A separate LLM call checks whether $\hat{a}$ is fully supported by $\tilde{c}$. If it is, $a$ is set to $\hat{a}$ and the pipeline terminates (method=per_chunk).   When no chunk clears all four checks, the fallback merges all expanded contexts into $\tilde{\mathcal{E}}$, generates a consolidated answer (method=fallback_merge), and runs one final groundedness check before anything is returned to the user.
7. or terminating retrieval and proceeding to answer generation. The reward $\mathcal{R}$ is shaped by answer groundedness (which is already measured at inference time), engineer satisfaction feedback collected through the Dash UI, citation accuracy, and a latency penalty to discourage unnecessarily long retrieval chains:   Equation $$ \mathcal{R}=\alpha\cdot\text{Groundedness}+\beta\cdot\text{Feedback}-\gamma_{l}\cdot\text{Latency} $$   To give this direction a concrete anchor, a composite system accuracy $\Phi$ is introduced that jointly captures retrieval quality and answer fidelity:   Equation $$ \Phi=0.30\cdot P@10\;+\;0.30\cdot R@10\;+\;0.40\cdot\text{Grounding} $$   Under the current rule-based configuration, plugging in the reported numbers gives:   Equation $$ \Phi_{\text{current}}=0.30\times 0.69+0.30\times 0.79+0.40\times 0.896\approx 80.2\% $$   An offline-trained Q-Learning policy, optimised on logged production trajectories, is projected to lift $P@10$ to approximately $0.76$ ($+7$pp), $R@10$ to approximately $0.86$ ($+7$pp), and the grounding rate to approximately $94.5\%$ ($+4.9$pp). These are deliberately conservative estimates: because DocuSearch already incorporates cross-encoder reranking, MMR selection, and a per-chunk sufficiency gate, the remaining headroom for any single optimisation layer is bounded. What the RL agent would learn to do differently from the current fixed policy is route precision-heavy queries through BM25 rather than relying solely on dense retrieval, expand KG neighbours more aggressively on recall-sensitive troubleshooting queries, and halt retrieval earlier when accumulated evidence already clears the sufficiency threshold — decisions that are currently hard-coded but could be learned from query-level feedback. Under these assumptions:   Equation $$ \Phi_{\text{RL}}=0.30\times 0.76+0.30\times 0.86+0.40\times 0.945\approx 86.4\% $$   This represents an overall improvement of approximately $\mathbf{6.2}$ percentage points, lifting composite accuracy from $\approx 80\%$ to $\approx 86\%$. Table[VI](#S9.T6 "TABLE VI ‣ IX-A Limitations and Future Work ‣ IX Conclusion ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") summarises the projected gains per metric.   TABLE VI: Projected Performance Gains with RL-Based Adaptive Retrieval \| Metric \| Current \| Projected (RL) \| Gain \| \| --- \| --- \| --- \| --- \| \| $P@10$ \| 0.69 \| 0.76 \| $+7$ pp \| \| $R@10$ \| 0.79 \| 0.86 \| $+7$ pp \| \| Grounding \| 89.6% \| 94.5% \| $+4.9$ pp \| \| $\Phi$ (composite) \| 80.2% \| 86.4% \| $\mathbf{+6.2}$ pp \|    Off-policy Q-Learning is a natural fit here. Document retrieval is largely deterministic — the same action
8. Table[V](#S8.T5 "TABLE V ‣ VIII-D Ablation Study ‣ VIII Results and Analysis ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") shows grounding rate when each component is removed individually, holding everything else constant.   TABLE V: Ablation Study: Grounding Rate   Groundedness verification has the largest individual impact at $15.3$ percentage points, followed by sufficiency scoring at $11.5$ points. It is worth noting that even the “smallest” contributors — vendor scoping at $3.5$ points and KG expansion at $4.4$ points — add up meaningfully in combination, indicating that no component is redundant.

## Response

## Results

| System | Grounding rate | Hallucination rate | Fallback |
|---|---:|---:|---:|
| Single-pass RAG baseline | **71.2%** | **21.4%** | — |
| DocuSearch | **89.6%** | **6.8%** | **3.6%** |

These figures come from the paper’s per-chunk evaluation comparison.[1][2]

- **Grounding rate** measures whether the generated answer is supported by the retrieved context: DocuSearch’s verifier accepts an answer only when it is fully supported by the expanded chunk.[6]
- **Hallucination rate** is the reported proportion of answers classified as hallucinated rather than grounded.[1]

Compared with single-pass RAG, DocuSearch improves grounding by **18.4 percentage points**, from 71.2% to 89.6%, and reduces hallucinations by **14.6 percentage points**, from 21.4% to 6.8%.[1][2] The remaining 3.6% of DocuSearch cases use a fallback that merges the expanded contexts before generating and checking a consolidated answer.[2][6]

### Limitations and ambiguity

The comparison is not completely transparent: the baseline row has no reported fallback category, while DocuSearch’s categories sum to 100% only when fallback cases are included.[1] Moreover, the paper says that fallback cases “still produce grounded answers,” but the table lists fallback separately from the 89.6% grounded category; therefore, the excerpt does not make clear whether the 89.6% grounding rate excludes or includes grounded fallback answers.[2] The reported rates should consequently be read as table-level results, not necessarily as directly comparable, mutually exhaustive outcome categories.
