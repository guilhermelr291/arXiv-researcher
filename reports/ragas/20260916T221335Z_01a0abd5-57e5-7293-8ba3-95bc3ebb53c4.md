# RAGAS Writer report `01a0abd5-57e5-7293-8ba3-95bc3ebb53c4`

- scored_at: `2026-09-16T22:13:35Z`
- project: `plan-based-researcher`
- judge: `gpt-4o-mini` + `text-embedding-3-small`

## Query

How much does the full DocuSearch system improve Precision@10 and Recall@10 relative to the dense-only retrieval baseline? (2609.01617)

## Faithfulness: 1.0

| verdict | statement | reason |
| --- | --- | --- |
| 1 | The retrieval results for Precision@10 show that the dense-only method has a value of 0.54. | The context states that the dense-only method has a Precision@10 value of 0.54. |
| 1 | The retrieval results for Precision@10 show that the full DocuSearch method has a value of 0.69. | The context explicitly states that the full DocuSearch method has a Precision@10 value of 0.69. |
| 1 | The absolute improvement in Precision@10 is calculated as 0.69 minus 0.54, resulting in 0.15. | The absolute improvement can be directly calculated from the values provided in the context. |
| 1 | The relative improvement in Precision@10 is calculated as 0.15 divided by 0.54, resulting in approximately 27.8%. | The relative improvement can be directly calculated from the absolute improvement and the dense-only value provided in the context. |
| 1 | The retrieval results for Recall@10 show that the dense-only method has a value of 0.63. | The context states that the dense-only method has a Recall@10 value of 0.63. |
| 1 | The retrieval results for Recall@10 show that the full DocuSearch method has a value of 0.79. | The context explicitly states that the full DocuSearch method has a Recall@10 value of 0.79. |
| 1 | The absolute improvement in Recall@10 is calculated as 0.79 minus 0.63, resulting in 0.16. | The absolute improvement can be directly calculated from the values provided in the context. |
| 1 | The relative improvement in Recall@10 is calculated as 0.16 divided by 0.63, resulting in approximately 25.4%. | The relative improvement can be directly calculated from the absolute improvement and the dense-only value provided in the context. |
| 1 | Full DocuSearch improves Precision@10 by 15 percentage points, which is about 27.8%. | The context states that the full DocuSearch improves Precision@10 by 15 percentage points, and the relative improvement is approximately 27.8%. |
| 1 | Full DocuSearch improves Recall@10 by 16 percentage points, which is about 25.4%. | The context states that the full DocuSearch improves Recall@10 by 16 percentage points, and the relative improvement is approximately 25.4%. |
| 1 | The paper explicitly reports these gains as 15 and 16 percentage points, respectively. | The context explicitly mentions that the paper reports these gains as 15 and 16 percentage points. |

## Answer relevancy: 0.904373825833043

| generated question | similarity | noncommittal |
| --- | --- | --- |
| What are the improvements in Precision@10 and Recall@10 when comparing full DocuSearch to the dense-only baseline? | 0.9044 | 0 |
| What are the improvements in Precision@10 and Recall@10 when comparing full DocuSearch to the dense-only baseline? | 0.9044 | 0 |
| What are the improvements in Precision@10 and Recall@10 when comparing full DocuSearch to the dense-only baseline? | 0.9044 | 0 |

## Retrieved contexts

1. Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search > VIII Results and Analysis > VIII-A Retrieval Quality TABLE III: Retrieval Quality Comparison \| Configuration \| P@5 \| R@5 \| P@10 \| R@10 \| \| --- \| --- \| --- \| --- \| --- \| \| Dense only \| 0.61 \| 0.48 \| 0.54 \| 0.63 \| \| Dense + BM25 \| 0.68 \| 0.56 \| 0.61 \| 0.71 \| \| DocuSearch (full) \| 0.76 \| 0.64 \| 0.69 \| 0.79 \|
2. Getting accurate, grounded answers out of large enterprise document repositories is a difficult problem. Dense vector retrieval alone frequently performs poorly on queries that mix technical terminology, vendor-specific acronyms, or require reasoning across several non-adjacent sections. DocuSearch was built to address exactly this gap — an offline, multi-agent document intelligence system developed and evaluated in a production telecom network operations environment. Rather than relying on a single retrieval signal, DocuSearch pulls together three complementary sources of evidence: semantic search over a Qdrant vector store using BGE-Large embeddings, BM25 full-text search over an SQLite FTS5 index, and Knowledge Graph neighbour expansion from a structured edge table. These three ranked lists are merged through Reciprocal Rank Fusion (RRF) with signal weights $w_{v}=0.50$, $w_{b}=0.35$, and $w_{kg}=0.15$, giving:   Equation $$ s_{\mathrm{RRF}}(d)\;=\;\sum_{i\in\{v,\,b,\,kg\}}\frac{w_{i}}{k+r_{i}(d)},\quad k=60 $$   where $r_{i}(d)$ is the rank of chunk $d$ in signal list $i$. A cross-encoder then reranks the fused list, and Maximal Marginal Relevance (MMR) with $\lambda=0.65$ prunes it for both relevance and diversity. What makes DocuSearch distinctive is a per-chunk evaluation loop that treats each selected chunk as its own mini-retrieval problem: an LLM decides whether the chunk needs additional neighbouring context, whether it is actually sufficient to answer the query, and whether the answer it generates is genuinely grounded in the retrieved text. Ungrounded answers are not returned; the system falls back to a multi-chunk merge instead. On an internal telecom document corpus, DocuSearch reaches Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\%$ — gains of $15$, $16$, and $18.4$ percentage points over a dense-only RAG baseline.
3. This paper presented DocuSearch, a hybrid multi-agent RAG system built for enterprise document intelligence in multi-vendor telecom environments. The system was designed around three failure modes observed in production: signal incompleteness, context fragmentation, and unverified answer grounding. The two-phase pipeline — hybrid retrieval fusion followed by per-chunk agentic evaluation — directly addresses all three. Results show Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\%$, with hallucinations dropping to $6.8\%$. The ablation confirms that groundedness verification and sufficiency scoring drive the largest improvements, though no single component is redundant. A key practical finding is that production-quality document intelligence is achievable entirely on locally hosted models. DocuSearch requires no external API calls, which matters a great deal in telecom operations environments where sensitive network configuration data cannot leave the organisation’s infrastructure.
4. or terminating retrieval and proceeding to answer generation. The reward $\mathcal{R}$ is shaped by answer groundedness (which is already measured at inference time), engineer satisfaction feedback collected through the Dash UI, citation accuracy, and a latency penalty to discourage unnecessarily long retrieval chains:   Equation $$ \mathcal{R}=\alpha\cdot\text{Groundedness}+\beta\cdot\text{Feedback}-\gamma_{l}\cdot\text{Latency} $$   To give this direction a concrete anchor, a composite system accuracy $\Phi$ is introduced that jointly captures retrieval quality and answer fidelity:   Equation $$ \Phi=0.30\cdot P@10\;+\;0.30\cdot R@10\;+\;0.40\cdot\text{Grounding} $$   Under the current rule-based configuration, plugging in the reported numbers gives:   Equation $$ \Phi_{\text{current}}=0.30\times 0.69+0.30\times 0.79+0.40\times 0.896\approx 80.2\% $$   An offline-trained Q-Learning policy, optimised on logged production trajectories, is projected to lift $P@10$ to approximately $0.76$ ($+7$pp), $R@10$ to approximately $0.86$ ($+7$pp), and the grounding rate to approximately $94.5\%$ ($+4.9$pp). These are deliberately conservative estimates: because DocuSearch already incorporates cross-encoder reranking, MMR selection, and a per-chunk sufficiency gate, the remaining headroom for any single optimisation layer is bounded. What the RL agent would learn to do differently from the current fixed policy is route precision-heavy queries through BM25 rather than relying solely on dense retrieval, expand KG neighbours more aggressively on recall-sensitive troubleshooting queries, and halt retrieval earlier when accumulated evidence already clears the sufficiency threshold — decisions that are currently hard-coded but could be learned from query-level feedback. Under these assumptions:   Equation $$ \Phi_{\text{RL}}=0.30\times 0.76+0.30\times 0.86+0.40\times 0.945\approx 86.4\% $$   This represents an overall improvement of approximately $\mathbf{6.2}$ percentage points, lifting composite accuracy from $\approx 80\%$ to $\approx 86\%$. Table[VI](#S9.T6 "TABLE VI ‣ IX-A Limitations and Future Work ‣ IX Conclusion ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") summarises the projected gains per metric.   TABLE VI: Projected Performance Gains with RL-Based Adaptive Retrieval \| Metric \| Current \| Projected (RL) \| Gain \| \| --- \| --- \| --- \| --- \| \| $P@10$ \| 0.69 \| 0.76 \| $+7$ pp \| \| $R@10$ \| 0.79 \| 0.86 \| $+7$ pp \| \| Grounding \| 89.6% \| 94.5% \| $+4.9$ pp \| \| $\Phi$ (composite) \| 80.2% \| 86.4% \| $\mathbf{+6.2}$ pp \|    Off-policy Q-Learning is a natural fit here. Document retrieval is largely deterministic — the same action

## Response

## Improvement over the dense-only baseline

The retrieval results are:

- **Precision@10:** dense-only = **0.54**; full DocuSearch = **0.69**【1】
- **Recall@10:** dense-only = **0.63**; full DocuSearch = **0.79**【1】

Therefore:

| Metric | Absolute improvement | Relative improvement |
|---|---:|---:|
| Precision@10 | \(0.69-0.54=\) **0.15** (**15 percentage points**) | \(0.15/0.54\approx\) **27.8%** |
| Recall@10 | \(0.79-0.63=\) **0.16** (**16 percentage points**) | \(0.16/0.63\approx\) **25.4%** |

Thus, full DocuSearch improves **Precision@10 by 15 percentage points (about 27.8%)** and **Recall@10 by 16 percentage points (about 25.4%)** relative to dense-only retrieval. The paper also explicitly reports these gains as **15 and 16 percentage points**, respectively.【2】
