# RAGAS Writer report `01a0abd1-a6ad-7130-a21f-6089e7def57d`

- scored_at: `2026-09-16T20:04:52Z`
- project: `plan-based-researcher`
- judge: `gpt-4o-mini` + `text-embedding-3-small`

## Query

What three retrieval signals does DocuSearch combine in its hybrid retrieval architecture? (2609.01617)

## Faithfulness: 1.0

| verdict | statement | reason |
| --- | --- | --- |
| 1 | DocuSearch combines three retrieval signals. | The context explicitly states that DocuSearch pulls together three complementary sources of evidence. |
| 1 | The first retrieval signal is dense semantic vector retrieval. | The context mentions that the first source of evidence is semantic search over a Qdrant vector store. |
| 1 | Dense semantic vector retrieval searches a Qdrant vector store using BGE-Large embeddings. | The context specifies that dense vector retrieval uses BGE-Large embeddings over a Qdrant vector store. |
| 1 | Dense semantic vector retrieval matches chunks by semantic similarity. | The context implies that dense vector retrieval is based on semantic search, which matches chunks by similarity. |
| 1 | The second retrieval signal is BM25 sparse/full-text retrieval. | The context clearly states that BM25 full-text search is one of the three sources of evidence. |
| 1 | BM25 sparse/full-text retrieval searches an SQLite FTS5 index using extracted keywords. | The context describes that BM25 scoring applies to an SQLite FTS5 index. |
| 1 | BM25 sparse/full-text retrieval helps match technical acronyms and model numbers. | The context indicates that BM25 is particularly useful for matching technical acronyms and model numbers. |
| 1 | The third retrieval signal is Knowledge Graph (KG) neighbour expansion. | The context explicitly states that Knowledge Graph neighbour expansion is the third source of evidence. |
| 1 | Knowledge Graph neighbour expansion follows edges from seed chunks. | The context mentions that KG neighbour expansion pulls in structurally connected chunks from seed chunks. |
| 1 | Knowledge Graph neighbour expansion retrieves structurally connected chunks that may be lexically or semantically distant. | The context describes that KG neighbour expansion retrieves chunks that are topically related but lexically and semantically distant. |
| 1 | The three ranked lists are merged using weighted Reciprocal Rank Fusion. | The context states that the three ranked lists are merged through Reciprocal Rank Fusion (RRF). |
| 1 | The weight for vector retrieval is 0.50. | The context provides the specific weight for vector retrieval as 0.50. |
| 1 | The weight for BM25 is 0.35. | The context specifies the weight for BM25 as 0.35. |
| 1 | The weight for KG expansion is 0.15. | The context clearly states the weight for KG expansion as 0.15. |
| 1 | The provided evidence consistently identifies these three signals. | The context describes the three retrieval signals and their contributions, indicating consistency. |
| 1 | No ambiguity is apparent regarding the three retrieval signals. | The context clearly outlines the three retrieval signals without any conflicting information. |

## Answer relevancy: 0.8790450614609019

| generated question | similarity | noncommittal |
| --- | --- | --- |
| What are the three retrieval signals used by DocuSearch? | 0.8790 | 0 |
| What are the three retrieval signals used by DocuSearch? | 0.8790 | 0 |
| What are the three retrieval signals used by DocuSearch? | 0.8790 | 0 |

## Retrieved contexts

1. Getting accurate, grounded answers out of large enterprise document repositories is a difficult problem. Dense vector retrieval alone frequently performs poorly on queries that mix technical terminology, vendor-specific acronyms, or require reasoning across several non-adjacent sections. DocuSearch was built to address exactly this gap — an offline, multi-agent document intelligence system developed and evaluated in a production telecom network operations environment. Rather than relying on a single retrieval signal, DocuSearch pulls together three complementary sources of evidence: semantic search over a Qdrant vector store using BGE-Large embeddings, BM25 full-text search over an SQLite FTS5 index, and Knowledge Graph neighbour expansion from a structured edge table. These three ranked lists are merged through Reciprocal Rank Fusion (RRF) with signal weights $w_{v}=0.50$, $w_{b}=0.35$, and $w_{kg}=0.15$, giving:   Equation $$ s_{\mathrm{RRF}}(d)\;=\;\sum_{i\in\{v,\,b,\,kg\}}\frac{w_{i}}{k+r_{i}(d)},\quad k=60 $$   where $r_{i}(d)$ is the rank of chunk $d$ in signal list $i$. A cross-encoder then reranks the fused list, and Maximal Marginal Relevance (MMR) with $\lambda=0.65$ prunes it for both relevance and diversity. What makes DocuSearch distinctive is a per-chunk evaluation loop that treats each selected chunk as its own mini-retrieval problem: an LLM decides whether the chunk needs additional neighbouring context, whether it is actually sufficient to answer the query, and whether the answer it generates is genuinely grounded in the retrieved text. Ungrounded answers are not returned; the system falls back to a multi-chunk merge instead. On an internal telecom document corpus, DocuSearch reaches Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\%$ — gains of $15$, $16$, and $18.4$ percentage points over a dense-only RAG baseline.
2. In telecom network operations centres, engineers frequently must search through hundreds of vendor manuals for a specific configuration procedure under time pressure. These environments accumulate vast repositories of technical documentation — SOPs, RCA reports, audit records, multi-vendor configuration guides — and the gap between what keyword search surfaces and what an engineer actually needs can be substantial. Conventional search returns too many loosely related hits. Standard single-signal Retrieval-Augmented Generation (RAG)[1], while an improvement, still struggles with specialised technical vocabulary, fragmented document chunks, and the reality that relevant information is often spread across non-adjacent sections from different vendors. This paper describes DocuSearch, a hybrid multi-agent RAG system designed and deployed within a real telecom network operations environment. Three limitations motivated its design: signal incompleteness, where a single retrieval function misses chunks that are relevant in semantic, lexical, and structural senses simultaneously; context fragmentation, where a retrieved chunk is only half of a procedure; and unverified grounding, where a generated answer sounds plausible but is not actually supported by the retrieved evidence. DocuSearch addresses all three by combining dense vector retrieval, BM25 sparse retrieval [2], and Knowledge Graph expansion[9] into a single fused ranking via Reciprocal Rank Fusion (RRF) [7], followed by cross-encoder reranking, Maximal Marginal Relevance (MMR) selection [3], and — the primary novel contribution — a per-chunk agentic evaluation loop that verifies context sufficiency and answer grounding before committing to a response. The pipeline runs entirely on locally hosted models and is orchestrated via LangGraph [5], exposed through an interactive Dash web application with vendor-scoped retrieval, session document upload, and integrated Knowledge and Learning Hub modules. The main contributions are as follows:   * •   A three-signal hybrid retrieval architecture fused via weighted RRF with signal weights $w_{v}{=}0.50,\;w_{b}{=}0.35,\;w_{kg}{=}0.15$. * •   A per-chunk agentic evaluation loop with LLM-driven context need detection, sufficiency scoring, and groundedness verification. * •   Vendor-aware retrieval scoping for multi-vendor
3. Five aspects of DocuSearch distinguish it from existing RAG systems. Per-Chunk Agentic Evaluation Loop. Most RAG pipelines concatenate all retrieved chunks and generate one answer in a single forward pass. DocuSearch instead evaluates each $c\in\mathcal{E}$ independently through four sequential checks. A chunk must score $\sigma(\tilde{c},q)\geq\tau=7$ before generation is even attempted, and the returned answer is explicitly verified as grounded before being committed. This eliminates a whole class of hallucinations that arise when an LLM is asked to synthesise an answer from a mixed bag of partially relevant context. Weighted Three-Signal RRF Fusion. Most hybrid RAG systems stop at two signals — typically dense plus sparse. A third, KG neighbour expansion, is added as an explicit signal in the RRF formula, with empirically tuned weights $w_{v}=0.50$, $w_{b}=0.35$, $w_{kg}=0.15$. The KG signal’s contribution is modest but consistent: it reliably surfaces structural neighbours that neither cosine similarity nor BM25 would rank highly on their own. Dynamic Neighbour Context Expansion. Rather than using larger fixed chunk sizes at indexing time (which reduces retrieval precision), DocuSearch detects fragmentation at query time. When a retrieved chunk is identified as a fragment, up to $L=2$ levels of neighbouring chunks are fetched from the metadata database on the fly, recovering coherent multi-step procedures without any pre-processing trade-offs. Vendor-Aware Retrieval Scoping. A vendor registry is built from the document metadata that maps each $v\in\mathcal{V}$ to its set of document identifiers. When the Vendor Detection agent identifies a vendor reference in the query, all three retrieval signals are scoped to that vendor’s documents. The noise reduction for vendor-specific configuration and troubleshooting queries is substantial. LangGraph-Orchestrated Agentic Pipeline. The entire pipeline runs as a stateful LangGraph DAG [5] with five explicitly defined nodes. Every intermediate state is observable, serialisable, and independently replaceable — which turned out to be practically important during development, since individual components could be swapped without disturbing anything upstream or downstream.
4. The Multi-Signal Retrieval Layer runs three parallel agents ($k_{v}{=}40$, $k_{b}{=}40$, $k_{kg}{=}15$), each optimised for a different aspect of relevance. Fusion, reranking ($k_{r}{=}12$), and evidence selection ($\|\mathcal{E}\|{=}10$) happen in the Ranking and Selection Layer. The Agentic Evaluation Layer then runs four sequential LLM checks on each candidate chunk, which accounts for most of the observed quality gains. Above all of this sits the Orchestration Layer, a LangGraph DAG with five nodes: query_analysis, retrieve, fuse_and_rank, evidence_select, and per_chunk_eval. Users interact through the User Interface Layer, a Plotly Dash application that exposes a conversational search interface alongside real-time pipeline visualisation, a Knowledge Hub, a Learning Hub, and document ingestion panels.
5. DocuSearch tackles the four sub-problems through a layered multi-agent pipeline split into two phases, illustrated in Figure[1](#S4.F1 "Figure 1 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") and Figure[2](#S4.F2 "Figure 2 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search"). Pre-MMR Phase. When a query $q$ arrives, a Template Selection agent first classifies it into one of five answer templates $\mathcal{T}$. A BM25 Keyword agent then pulls out domain-specific terms, and a Vendor Detection agent optionally narrows all three retrieval signals to a specific vendor namespace $v\in\mathcal{V}$. Dense vector search, BM25 full-text search, and KG neighbour expansion run in parallel, and their ranked results are merged by weighted RRF, reranked by a cross-encoder, and filtered by MMR to produce the evidence set $\mathcal{E}$. Post-MMR Phase. Each chunk $c\in\mathcal{E}$ is evaluated individually by four sequential LLM-driven nodes: context need detection, sufficiency scoring against a threshold $\tau=7$, answer generation, and groundedness verification. The first chunk that clears all four checks produces the final answer. When no individual chunk passes, a fallback merges all expanded contexts, generates a consolidated answer, and runs one final groundedness check before returning anything to the user.
6. Dense Vector Retrieval. The query $q$ is encoded using BGE-Large-EN-v1.5 [4] to get $\mathbf{q}\in\mathbb{R}^{1024}$. Cosine similarity is computed against all chunk embeddings in Qdrant, returning the top $k_{v}=40$ chunks:   Equation $$ \mathrm{score}_{v}(c)=\frac{\mathbf{q}\cdot\mathbf{e}_{c}}{\\|\mathbf{q}\\|\cdot\\|\mathbf{e}_{c}\\|} $$   BM25 Sparse Retrieval. Extracted keywords query the SQLite FTS5 index. Standard BM25 scoring applies:   Equation $$ \mathrm{score}_{b}(c,Q)=\sum_{q_{i}\in Q}\mathrm{IDF}(q_{i})\cdot\frac{f(q_{i},c)(k_{1}+1)}{f(q_{i},c)+k_{1}(1-b+b\cdot\frac{\|c\|}{avgdl})} $$   with $k_{1}=1.2$, $b=0.75$, where $\|c\|$ is chunk length and $avgdl$ is the average chunk length across $\mathcal{C}$. Top $k_{b}=40$ chunks are retrieved. BM25 is particularly useful here because technical acronyms and model numbers that have no meaningful semantic neighbourhood still produce strong lexical matches. KG Neighbour Expansion. Seed chunks from the vector and BM25 results traverse a KG edge table stored in SQLite, pulling in structurally connected chunks (top $k_{kg}=15$). This catches content that is topically related but lexically and semantically distant — for instance, a configuration snippet that references a procedure described in a different section.
7. The system is organised across six functional tiers spanning the two phases shown in Figures[1](#S4.F1 "Figure 1 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") and[2](#S4.F2 "Figure 2 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search").   Figure 1: Pre-MMR Phase ( run_supervisor ): Step 1 — Template Selection (LLM call); Step 2 — BM25 Keyword Extraction (LLM + Rule Based); Step 2b — Vendor Detection (LLM call); Steps 3/4/5 — Parallel Vector Search, BM25 Retrieval, KG Expansion; Step 5b — RRF Fusion ( w v = 0.50 , w b = 0.35 , w k  g = 0.15 w_{v}{=}0.50,\,w_{b}{=}0.35,\,w_{kg}{=}0.15 ); Step 6 — Cross-Encoder Reranking; Step 7 — MMR Selection ( λ = 0.65 \lambda{=}0.65 , relevance + diversity). Output: ordered MMR chunks passed to Post-MMR phase. Figure 2: Post-MMR Phase ( run_per_chunk_evaluation ): Per-chunk loop — LLM evaluates current chunk; if more context needed, neighbouring chunks are fetched (green path); sufficiency check gates answer generation; groundedness LLM verifies the generated answer; if grounded, pipeline terminates ( method=per_chunk ); otherwise retries next MMR chunk. When all MMR chunks are exhausted, fallback merges all expanded contexts and returns a consolidated answer ( method=fallback_merge ).   The Document Ingestion Layer handles parsing and chunking, producing overlapping segments ($s=900$ chars, $\delta=140$ chars) stored in both Qdrant[6] and SQLite FTS5. The Multi-Signal Retrieval Layer runs three parallel agents ($k_{v}{=}40$, $k_{b}{=}40$, $k_{kg}{=}15$), each

## Response

DocuSearch combines these **three retrieval signals**:

1. **Dense semantic vector retrieval** — searches a Qdrant vector store using BGE-Large embeddings, matching chunks by semantic similarity. [1][6]
2. **BM25 sparse/full-text retrieval** — searches an SQLite FTS5 index using extracted keywords, which helps match technical acronyms and model numbers. [1][6]
3. **Knowledge Graph (KG) neighbour expansion** — follows edges from seed chunks to retrieve structurally connected chunks that may be lexically or semantically distant. [1][6]

The three ranked lists are merged using weighted Reciprocal Rank Fusion, with weights \(0.50\) for vector retrieval, \(0.35\) for BM25, and \(0.15\) for KG expansion. [2][7]

**Ambiguity or missing information:** The provided evidence consistently identifies these same three signals; no ambiguity is apparent.
