# RAGAS Writer report `01a0abd7-7af0-79e3-b4c4-e9f3a81803da`

- scored_at: `2026-09-16T22:00:07Z`
- project: `plan-based-researcher`
- judge: `gpt-4o-mini` + `text-embedding-3-small`

## Query

How does DocuSearch's dynamic neighbour context expansion avoid the trade-offs of simply using larger fixed chunk sizes at indexing time? (2609.01617)

## Faithfulness: 0.6486486486486487

| verdict | statement | reason |
| --- | --- | --- |
| 1 | DocuSearch keeps documents divided into relatively small, overlapping chunks at indexing time. | The context explicitly states that documents are parsed and chunked into overlapping segments. |
| 1 | Each chunk consists of 900 characters with a 140-character overlap. | The context specifies that chunks are stored with 900 characters and a 140-character overlap. |
| 1 | Experiments found that larger chunks degraded retrieval precision. | The context mentions that larger chunks hurt retrieval precision based on experimentation. |
| 1 | Smaller chunks could hurt answer sufficiency. | The context indicates that smaller chunks hurt sufficiency scoring, which is directly mentioned. |
| 1 | The key idea is to separate retrieval granularity from answering context. | The context discusses the importance of retrieval granularity and its relation to answering context. |
| 1 | For a query, DocuSearch combines vector search, BM25 search, and knowledge-graph neighbour expansion using weighted Reciprocal Rank Fusion. | The context describes how DocuSearch integrates multiple retrieval signals including vector search and BM25. |
| 1 | A cross-encoder reranks the results. | The context explicitly states that a cross-encoder is used to rerank the results. |
| 1 | MMR selects relevant and diverse chunks. | The context mentions that MMR is used to select relevant and diverse chunks. |
| 1 | In the per-chunk evaluation loop, an LLM checks whether the current retrieved chunk needs more context. | The context describes the per-chunk evaluation loop where an LLM evaluates the need for more context. |
| 1 | If a chunk is a fragment, DocuSearch fetches neighbouring chunks from the metadata database, by up to two neighbour levels. | The context states that if a chunk is identified as a fragment, neighbouring chunks are fetched from the metadata database. |
| 1 | The system does not give every query a large context. | The context indicates that the system does not provide a large context for every query. |
| 1 | An individual chunk expands only when the evaluation process identifies that additional context is needed. | The context specifies that chunk expansion occurs only when additional context is deemed necessary. |
| 1 | This process can recover a complete multi-step procedure when relevant information crosses chunk boundaries. | The context mentions that the system can recover coherent multi-step procedures across chunk boundaries. |
| 1 | The precision benefits of smaller indexed chunks are retained. | The context discusses the retention of precision benefits with smaller indexed chunks. |
| 1 | After expansion, the system checks whether the evidence is sufficient before attempting generation. | The context states that the system verifies evidence sufficiency before generating an answer. |
| 1 | The system verifies whether the generated answer is grounded. | The context explicitly mentions that the system checks if the generated answer is grounded. |
| 1 | If the answer is not grounded, the system tries another selected chunk or eventually uses a fallback merge of the expanded contexts. | The context describes the fallback mechanism if the answer is not grounded. |
| 0 | Large fixed chunks would make more neighbouring text available from the beginning. | The context implies that larger fixed chunks would provide more context initially, but does not state this as a fact. |
| 0 | The paper reports that larger fixed chunks reduce retrieval precision. | The context mentions that larger chunks degrade retrieval precision, but does not specify this as a report from the paper. |
| 0 | Dynamic expansion supplies additional context specifically for fragmented results. | The context discusses dynamic expansion but does not explicitly state that it is only for fragmented results. |
| 0 | Dynamic expansion helps recover coherent procedures without making every indexed unit large. | The context mentions dynamic expansion but does not specify that it helps recover procedures without large indexed units. |
| 0 | Fixed large chunks impose the same broad context size on every query. | The context discusses fixed chunk sizes but does not state that they impose the same context size on every query. |
| 0 | DocuSearch preserves fine-grained chunks for retrieval. | The context mentions the use of overlapping chunks but does not explicitly state that fine-grained chunks are preserved. |
| 0 | The context enlarges only for chunks judged to be incomplete. | The context states that context enlarges for incomplete chunks but does not specify that it is the only condition. |
| 0 | The indexed representation remains granular. | The context discusses chunking but does not explicitly state that the indexed representation remains granular. |
| 0 | The effective context can become larger when the query requires a multi-chunk procedure. | The context implies that context can become larger but does not state this as a definitive fact. |
| 1 | The document is indexed once as overlapping 900-character chunks in both Qdrant and SQLite FTS5. | The context explicitly states that documents are indexed as overlapping chunks in both systems. |
| 1 | Neighbours are fetched later from the metadata database during retrieval. | The context states that neighbouring chunks are fetched from the metadata database during retrieval. |
| 0 | Neighbours are not incorporated into permanently larger indexed chunks. | The context mentions that neighbours are fetched but does not state that they are not incorporated into larger chunks. |
| 0 | The paper describes this as avoiding the pre-processing trade-offs associated with larger fixed chunks. | The context discusses avoiding pre-processing trade-offs but does not specify that this is described in the paper. |
| 0 | The paper does not provide a separate quantitative measurement of the additional query-time computation, metadata lookups, or storage savings produced by dynamic expansion compared with indexing larger fixed chunks. | The context does not mention any quantitative measurements provided by the paper. |
| 0 | The evidence supports the design rationale and workflow, but not a precise cost or storage comparison. | The context mentions evidence supporting the design rationale but does not specify that it lacks precise comparisons. |
| 1 | On the reported internal telecom corpus, the complete DocuSearch pipeline achieved Precision@10 of 0.69. | The context explicitly states the Precision@10 achieved by the DocuSearch pipeline. |
| 1 | The complete DocuSearch pipeline achieved Recall@10 of 0.79. | The context explicitly states the Recall@10 achieved by the DocuSearch pipeline. |
| 1 | The complete DocuSearch pipeline achieved an 89.6% grounding rate. | The context explicitly states the grounding rate achieved by the DocuSearch pipeline. |
| 1 | The complete DocuSearch pipeline outperformed the paper’s dense-only RAG baseline by 15, 16, and 18.4 percentage points respectively. | The context explicitly states the performance improvements over the dense-only RAG baseline. |
| 1 | These results concern the full system, so they do not isolate the contribution of dynamic neighbour expansion alone. | The context mentions that the results do not isolate the contribution of dynamic neighbour expansion. |

## Answer relevancy: 0.7674119048878354

| generated question | similarity | noncommittal |
| --- | --- | --- |
| How does DocuSearch avoid the large-fixed-chunk trade-off? | 0.7674 | 0 |
| How does DocuSearch avoid the large-fixed-chunk trade-off? | 0.7674 | 0 |
| How does DocuSearch avoid the large-fixed-chunk trade-off? | 0.7674 | 0 |

## Retrieved contexts

1. Five aspects of DocuSearch distinguish it from existing RAG systems. Per-Chunk Agentic Evaluation Loop. Most RAG pipelines concatenate all retrieved chunks and generate one answer in a single forward pass. DocuSearch instead evaluates each $c\in\mathcal{E}$ independently through four sequential checks. A chunk must score $\sigma(\tilde{c},q)\geq\tau=7$ before generation is even attempted, and the returned answer is explicitly verified as grounded before being committed. This eliminates a whole class of hallucinations that arise when an LLM is asked to synthesise an answer from a mixed bag of partially relevant context. Weighted Three-Signal RRF Fusion. Most hybrid RAG systems stop at two signals — typically dense plus sparse. A third, KG neighbour expansion, is added as an explicit signal in the RRF formula, with empirically tuned weights $w_{v}=0.50$, $w_{b}=0.35$, $w_{kg}=0.15$. The KG signal’s contribution is modest but consistent: it reliably surfaces structural neighbours that neither cosine similarity nor BM25 would rank highly on their own. Dynamic Neighbour Context Expansion. Rather than using larger fixed chunk sizes at indexing time (which reduces retrieval precision), DocuSearch detects fragmentation at query time. When a retrieved chunk is identified as a fragment, up to $L=2$ levels of neighbouring chunks are fetched from the metadata database on the fly, recovering coherent multi-step procedures without any pre-processing trade-offs. Vendor-Aware Retrieval Scoping. A vendor registry is built from the document metadata that maps each $v\in\mathcal{V}$ to its set of document identifiers. When the Vendor Detection agent identifies a vendor reference in the query, all three retrieval signals are scoped to that vendor’s documents. The noise reduction for vendor-specific configuration and troubleshooting queries is substantial. LangGraph-Orchestrated Agentic Pipeline. The entire pipeline runs as a stateful LangGraph DAG [5] with five explicitly defined nodes. Every intermediate state is observable, serialisable, and independently replaceable — which turned out to be practically important during development, since individual components could be swapped without disturbing anything upstream or downstream.
2. DocuSearch is implemented entirely in Python3 and served from a single application. Table[I](#S7.T1 "TABLE I ‣ VII Implementation Details ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") summarises the core technology stack.   TABLE I: DocuSearch Core Technology Stack \| Component \| Technology \| \| --- \| --- \| \| Vector Store \| Qdrant (self-hosted) \| \| Embedding \| BGE-Large-EN-v1.5 ($d{=}1024$, CUDA) \| \| Reranker \| Cross-Encoder (CUDA) \| \| FTS Index \| SQLite FTS5 \| \| Metadata Store \| SQLite \| \| LLM Inference \| Local REST or vLLM \| \| LLM Model \| Mistral [10] \| \| Pipeline Graph \| LangGraph \| \| UI Framework \| Plotly Dash \| \| Doc Parsing \| Docling, PyMuPDF, python-docx, openpyxl \| \| Auto Ingest \| watchdog directory monitor \|    Documents reach the system through three paths: server-side path submission, browser upload via Dash dcc.Upload, or automatic detection through a watchdog directory monitor. All formats are parsed and chunked with $s=900$ characters and $\delta=140$ characters overlap. These values were settled on after experimenting with several chunk sizes; smaller chunks hurt sufficiency scoring while larger ones degraded retrieval precision. Table[II](#S7.T2 "TABLE II ‣ VII Implementation Details ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") summarises the full retrieval configuration used across all experiments.   TABLE II: Default Retrieval and Ranking Configuration \| Parameter \| Symbol \| Value \| \| --- \| --- \| --- \| \| Vector top-$k$ \| $k_{v}$ \| 40 \| \| BM25 top-$k$ \| $k_{b}$ \| 40 \| \| KG top-$k$ \| $k_{kg}$ \| 15 \| \| Rerank top-$k$ \| $k_{r}$ \| 12 \| \| Final evidence \| $\|\mathcal{E}\|$ \| 10 \| \| RRF smoothing \| $k$ \| 60 \| \| RRF vector weight \| $w_{v}$ \| 0.50 \| \| RRF BM25 weight \| $w_{b}$ \| 0.35 \| \| RRF KG weight \| $w_{kg}$ \| 0.15 \| \| MMR lambda \| $\lambda$ \| 0.65 \| \| Sufficiency thresh. \| $\tau$ \| 7 \| \| Max neighbour loops \| $L$ \| 2 \| \| Chunk size \| $s$ \| 900 chars \| \| Chunk overlap \| $\delta$ \| 140 chars \| \| LLM temperature \| $T$ \| 0.0 \|    Session-only uploads are also supported: documents uploaded during a session are embedded in memory using a local NumPy array (max $90{,}000$ chars, top-$k_{u}=8$) without being written to the persistent knowledge base. All LLM calls use $T=0.0$ to keep outputs deterministic across repeated queries, which is important for reproducibility in an operational setting.
3. Getting accurate, grounded answers out of large enterprise document repositories is a difficult problem. Dense vector retrieval alone frequently performs poorly on queries that mix technical terminology, vendor-specific acronyms, or require reasoning across several non-adjacent sections. DocuSearch was built to address exactly this gap — an offline, multi-agent document intelligence system developed and evaluated in a production telecom network operations environment. Rather than relying on a single retrieval signal, DocuSearch pulls together three complementary sources of evidence: semantic search over a Qdrant vector store using BGE-Large embeddings, BM25 full-text search over an SQLite FTS5 index, and Knowledge Graph neighbour expansion from a structured edge table. These three ranked lists are merged through Reciprocal Rank Fusion (RRF) with signal weights $w_{v}=0.50$, $w_{b}=0.35$, and $w_{kg}=0.15$, giving:   Equation $$ s_{\mathrm{RRF}}(d)\;=\;\sum_{i\in\{v,\,b,\,kg\}}\frac{w_{i}}{k+r_{i}(d)},\quad k=60 $$   where $r_{i}(d)$ is the rank of chunk $d$ in signal list $i$. A cross-encoder then reranks the fused list, and Maximal Marginal Relevance (MMR) with $\lambda=0.65$ prunes it for both relevance and diversity. What makes DocuSearch distinctive is a per-chunk evaluation loop that treats each selected chunk as its own mini-retrieval problem: an LLM decides whether the chunk needs additional neighbouring context, whether it is actually sufficient to answer the query, and whether the answer it generates is genuinely grounded in the retrieved text. Ungrounded answers are not returned; the system falls back to a multi-chunk merge instead. On an internal telecom document corpus, DocuSearch reaches Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\%$ — gains of $15$, $16$, and $18.4$ percentage points over a dense-only RAG baseline.
4. Let $\mathcal{D}=\{d_{1},d_{2},\ldots,d_{N}\}$ be a large heterogeneous corpus of enterprise documents, where each document $d_{i}$ belongs to one or more vendor namespaces $\mathcal{V}=\{v_{1},v_{2},\ldots,v_{M}\}$. Each document is pre-processed into overlapping text chunks:   Equation $$ \mathcal{C}=\bigcup_{i=1}^{N}\bigl\{c_{i,1},\,c_{i,2},\,\ldots,\,c_{i,k_{i}}\bigr\} $$   where $c_{i,j}$ is the $j$-th chunk of $d_{i}$ and $k_{i}$ is the total chunk count for that document. Given a natural language query $q$, the goal is to retrieve a subset $\mathcal{E}\subset\mathcal{C}$ and generate an answer $a$ such that:   Equation $$ a=\mathcal{G}\!\left(q,\;\mathcal{E}\right) $$   where $\mathcal{G}$ synthesises the answer strictly from retrieved evidence, without hallucinating content that does not appear in $\mathcal{E}$. This deceptively simple formulation hides four concrete sub-problems that standard pipelines fail to handle:   1. 1.   Signal incompleteness. No single retrieval function $f:q\rightarrow\mathcal{C}$ can capture all relevant chunks, because relevance spans semantic, lexical, and structural dimensions at the same time. 2. 2.   Chunk fragmentation. A chunk $c_{i,j}$ retrieved in isolation is often incomplete when the relevant procedure spans $c_{i,j-1}$ through $c_{i,j+1}$. 3. 3.   Redundancy in evidence. Naive top-$k$ selection tends to return clusters of near-duplicate chunks, squandering context window capacity on repeated information. 4. 4.   Unverified grounding. Standard RAG hands retrieved chunks to $\mathcal{G}$ without ever checking whether the generated answer $a$ is genuinely supported by $\mathcal{E}$, which is a direct path to confident but wrong responses.   DocuSearch addresses all four within a unified pipeline that runs entirely on locally hosted models with no dependency on external API services.
5. The system is organised across six functional tiers spanning the two phases shown in Figures[1](#S4.F1 "Figure 1 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search") and[2](#S4.F2 "Figure 2 ‣ IV High-Level Architecture ‣ Hybrid Retrieval-Augmented Generation with Knowledge Graph Expansion, RRF Fusion, and Per-Chunk Grounded Evaluation for Enterprise Document Search").   Figure 1: Pre-MMR Phase ( run_supervisor ): Step 1 — Template Selection (LLM call); Step 2 — BM25 Keyword Extraction (LLM + Rule Based); Step 2b — Vendor Detection (LLM call); Steps 3/4/5 — Parallel Vector Search, BM25 Retrieval, KG Expansion; Step 5b — RRF Fusion ( w v = 0.50 , w b = 0.35 , w k  g = 0.15 w_{v}{=}0.50,\,w_{b}{=}0.35,\,w_{kg}{=}0.15 ); Step 6 — Cross-Encoder Reranking; Step 7 — MMR Selection ( λ = 0.65 \lambda{=}0.65 , relevance + diversity). Output: ordered MMR chunks passed to Post-MMR phase. Figure 2: Post-MMR Phase ( run_per_chunk_evaluation ): Per-chunk loop — LLM evaluates current chunk; if more context needed, neighbouring chunks are fetched (green path); sufficiency check gates answer generation; groundedness LLM verifies the generated answer; if grounded, pipeline terminates ( method=per_chunk ); otherwise retries next MMR chunk. When all MMR chunks are exhausted, fallback merges all expanded contexts and returns a consolidated answer ( method=fallback_merge ).   The Document Ingestion Layer handles parsing and chunking, producing overlapping segments ($s=900$ chars, $\delta=140$ chars) stored in both Qdrant[6] and SQLite FTS5. The Multi-Signal Retrieval Layer runs three parallel agents ($k_{v}{=}40$, $k_{b}{=}40$, $k_{kg}{=}15$), each

## Response

# How DocuSearch avoids the large-fixed-chunk trade-off

DocuSearch keeps documents divided into relatively small, overlapping chunks at indexing time—900 characters with a 140-character overlap—because experiments found that larger chunks degraded retrieval precision, while smaller chunks could hurt answer sufficiency. [2]

The key idea is to separate **retrieval granularity** from **answering context**:

1. **Retrieve precise chunks first.**  
   For a query, DocuSearch combines vector search, BM25 search, and knowledge-graph neighbour expansion using weighted Reciprocal Rank Fusion. A cross-encoder then reranks the results, and MMR selects relevant and diverse chunks. [3] [5]

2. **Detect incomplete chunks at query time.**  
   In the per-chunk evaluation loop, an LLM checks whether the current retrieved chunk needs more context. If it is a fragment, DocuSearch fetches neighbouring chunks from the metadata database, by up to two neighbour levels. [1] [2] [5]

3. **Expand only when necessary.**  
   The system therefore does not give every query a large context. It expands an individual chunk only when the evaluation process identifies that additional context is needed. This can recover a complete multi-step procedure when the relevant information crosses chunk boundaries, while retaining the precision benefits of smaller indexed chunks. [1] [4] [5]

4. **Check sufficiency and grounding.**  
   After expansion, the system checks whether the evidence is sufficient before attempting generation. It also verifies whether the generated answer is grounded; if the answer is not grounded, the system tries another selected chunk or eventually uses a fallback merge of the expanded contexts. [1] [3] [5]

## Why this is preferable to simply indexing larger chunks

### Context quality

Large fixed chunks would make more neighbouring text available from the beginning, but the paper reports that this reduces retrieval precision. [1] [2] Dynamic expansion instead supplies additional context specifically for fragmented results, helping recover coherent procedures without making every indexed unit large. [1]

### Granularity

Fixed large chunks impose the same broad context size on every query. DocuSearch preserves fine-grained chunks for retrieval and enlarges the context only for chunks judged to be incomplete. Thus, the indexed representation remains granular, while the effective context can become larger when the query requires a multi-chunk procedure. [1] [4] [5]

### Retrieval and storage behavior

The document is indexed once as overlapping 900-character chunks in both Qdrant and SQLite FTS5. Neighbours are fetched later from the metadata database during retrieval rather than being incorporated into permanently larger indexed chunks. [2] [5] The paper describes this as avoiding the “pre-processing trade-offs” associated with larger fixed chunks. [1]

The paper does **not** provide a separate quantitative measurement of the additional query-time computation, metadata lookups, or storage savings produced by dynamic expansion compared with indexing larger fixed chunks. Therefore, the evidence supports the design rationale and workflow, but not a precise cost or storage comparison.

## Supporting evidence

On the reported internal telecom corpus, the complete DocuSearch pipeline achieved Precision@10 of 0.69, Recall@10 of 0.79, and an 89.6% grounding rate, outperforming the paper’s dense-only RAG baseline by 15, 16, and 18.4 percentage points respectively. [3] These results concern the full system, so they do not isolate the contribution of dynamic neighbour expansion alone.
